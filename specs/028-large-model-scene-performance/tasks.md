# Tasks: Large Model Scene Performance

**Input**: Design documents from `/specs/028-large-model-scene-performance/`
**Prerequisites**: `plan.md`, `spec.md`
**Tests**: Required because this is a performance bug fix with persistent cache behavior.

## Phase 1: Setup

**Purpose**: Confirm the affected runtime/editor paths and create a focused validation target.

- [x] T001 Identify imported model prefab generation and editor deferred mesh binding paths in `Runtime/Engine/Assets/ModelPrefabBuilder.cpp` and `Project/Editor/Core/EditorActions.cpp`
- [x] T002 Identify editor viewport and default scene camera initialization paths in `Project/Editor/Panels/AViewControllable.cpp` and `Runtime/Engine/SceneSystem/SceneManager.cpp`
- [x] T003 Identify per-frame mesh RHI access in `Runtime/Rendering/Core/ABaseRenderer.cpp` and mesh ownership in `Runtime/Rendering/Resources/Mesh.h`

## Phase 2: User Story 1 - Imported Models Stay Interactive In Scene (Priority: P1)

**Goal**: Imported model instances participate in normal culling and avoid per-frame RHI adapter allocations.

**Independent Test**: Focused unit tests verify prefab culling defaults, editor camera culling defaults, and stable RHI mesh reuse.

### Tests

- [x] T004 [P] [US1] Add generated model prefab culling assertion in `Tests/Unit/AssetPrefabPipelineTests.cpp`
- [x] T005 [P] [US1] Add editor render path contract checks for culling defaults in `Tests/Unit/EditorRenderPathContractTests.cpp`
- [x] T006 [P] [US1] Add stable mesh RHI adapter reuse test in `Tests/Unit/MeshBoundingSphereTests.cpp`

### Implementation

- [x] T007 [US1] Set generated model prefab `frustumBehaviour` to `CULL_MODEL` in `Runtime/Engine/Assets/ModelPrefabBuilder.cpp`
- [x] T008 [US1] Set editor deferred imported mesh bindings to `CULL_MODEL` in `Project/Editor/Core/EditorActions.cpp`
- [x] T009 [US1] Enable geometry culling for editor view cameras in `Project/Editor/Panels/AViewControllable.cpp`
- [x] T010 [US1] Enable geometry culling for default scene main cameras in `Runtime/Engine/SceneSystem/SceneManager.cpp`
- [x] T011 [US1] Store and return a stable RHI mesh adapter from `Runtime/Rendering/Resources/Mesh.h` and `Runtime/Rendering/Resources/Mesh.cpp`

## Phase 3: User Story 2 - Existing Cached Imported Models Refresh Safely (Priority: P2)

**Goal**: Existing generated model prefab artifacts with disabled culling are invalidated and regenerated.

**Independent Test**: Asset import pipeline test confirms the current model-scene importer version changed.

### Tests

- [x] T012 [P] [US2] Assert current model-scene importer version in `Tests/Unit/AssetImportPipelineTests.cpp`

### Implementation

- [x] T013 [US2] Bump model-scene importer version in `Runtime/Core/Assets/AssetMeta.cpp`

## Phase 4: Validation

**Purpose**: Prove the focused contracts and affected build target still pass.

- [x] T014 Build `NullusUnitTests` Release target
- [x] T015 Run focused culling/importer/RHI adapter tests
- [x] T016 Run focused asset cache scan/import tests
- [x] T017 Complete plan-review quality gate or document environment limitation

## Phase 5: User Story 3 - Scene Draw Collection Scales With Many Meshes (Priority: P2)

**Goal**: Keep `ParseScene()` from allocating a sorted tree node for every drawable while preserving existing renderer iteration order.

**Independent Test**: Editor render path contract tests prove drawables use contiguous storage and explicit sorting, while existing render framework/debug lifecycle tests prove consumers still see the expected draw counts and deferred GBuffer setup.

### Tests

- [x] T018 [US3] Add drawable collection contract test in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: test first failed on 2026-05-18 because `BaseSceneRenderer.h` still used `std::multimap<float, Drawable>` and `ParseScene()` inserted with `.emplace(distanceToActor, drawable)`.
- [x] T019 [US3] Update GBuffer/static lifecycle contracts for the new drawable iteration form in `Tests/Unit/RenderFrameworkContractTests.cpp` and `Tests/Unit/DebugSceneLifecycleTests.cpp`.
- [x] T024 [US3] Extend render-path contract coverage so `ParseScene()` default material fallback uses non-loading `GetResource(":Materials\\Default.mat", false)` rather than `MaterialManager::operator[]`.

### Implementation

- [x] T020 [US3] Replace `BaseSceneRenderer` drawable containers with `std::vector<std::pair<float, Drawable>>` in `Runtime/Engine/Rendering/BaseSceneRenderer.h`.
- [x] T021 [US3] Collect drawables with `emplace_back` and sort opaque, transparent, and skybox lists after collection in `Runtime/Engine/Rendering/BaseSceneRenderer.cpp`.
- [x] T022 [US3] Update forward and deferred scene renderers to consume sorted drawable vectors in `Runtime/Engine/Rendering/ForwardSceneRenderer.cpp` and `Runtime/Engine/Rendering/DeferredSceneRenderer.cpp`.
- [x] T025 [US3] Keep default material fallback lookup non-loading in `Runtime/Engine/Rendering/BaseSceneRenderer.cpp` and move drawables into contiguous storage after descriptor population.
- [x] T023 [US3] Run focused validation. Evidence: `cmake --build Build --target NullusUnitTests --config Debug` completed successfully on 2026-05-18. Focused Debug test run passed `EditorRenderPathContractTests.SceneRenderFrameDoesNotLoadRendererAssetsInsideMainPass`, `EditorRenderPathContractTests.SceneDrawableCollectionAvoidsNodeAllocatedMultimapSort`, `RenderFrameworkContractTests.DeferredThreadedGBufferCaptureUsesPassBindingPlaceholder`, `DebugSceneLifecycleTests.SceneVisibilityRefreshPreservesEditorHelperDrawCount`, `AssetPrefabPipelineTests.BuildsGeneratedModelPrefabHierarchyWithRendererAssetReferences`, `EditorRenderPathContractTests.GeneratedModelResourceResolutionUsesPrefabInstanceMappings`, `EditorRenderPathContractTests.EditorViewportCamerasEnableGeometryFrustumCullingForLargeImportedModels`, `MeshResourceTests.*`, and `AssetImportPipelineTests.DefaultRegistrySelectsSceneImportersByExtension`.
- [x] T026 [US3] Complete plan-review gate. Evidence: R1 independent review found the default fallback material could synchronously load in `ParseScene()` and the issue was fixed with non-loading `GetResource`; R2 deeper audit found 0 P0/P1 and only P2 follow-ups for mesh-count reserve precision and behavior-level ordering tests.

## Dependencies & Execution Order

- Setup tasks precede implementation.
- US1 is the MVP and fixes the visible scene interaction regression.
- US2 depends on US1 behavior being decided because the importer version invalidates old generated prefab semantics.
- US3 depends on US1 only for the stable mesh/culling baseline; it is otherwise an independent renderer hot-path allocation reduction.
- Validation follows all code and spec changes.

## Notes

- RenderDoc evidence is not required for this specific CPU/editor render preparation fix because no GPU pass or backend synchronization behavior is changed.
- Runtime rendering correctness claims are limited to the tested contracts and build target.
- The current trace still shows `UIManager::DrawCanvas` / `Canvas::Draw` as the largest measured editor hotspot; hierarchy/UI virtualization remains a follow-up phase after per-panel scopes identify the exact panel cost.

## Phase 6: User Story 4 - Editor UI Draw Scales With Large Hierarchies (Priority: P2)

**Goal**: Improve the editor UI hot path for large imported scenes and add panel-level profiling evidence for the next optimization pass.

**Independent Test**: Editor render path contract tests verify cached per-panel scopes, cached TreeNode ImGui labels, and dirty-gated widget garbage collection; panel hook tests verify dirty collection still removes destroyed children.

### Tests

- [x] T027 [US4] Add panel scope and TreeNode label-cache contract coverage in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: test first failed on 2026-05-18 because `APanel::Draw()` still used the generic `APanel::DrawPanel` scope and `TreeNode::_Draw_Impl()` rebuilt `const std::string label = name + "###" + m_widgetID` every draw; later RED coverage failed while TreeNode draw still invoked the dynamic `textColorProvider` hot-path callback.
- [x] T028 [US4] Add dirty-gated widget garbage collection contract coverage in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: test first failed on 2026-05-18 because `WidgetContainer::CollectGarbages()` scanned every call and `AWidget::Destroy()` did not mark the parent container dirty.
- [x] T029 [US4] Add behavior coverage for dirty-gated collection in `Tests/Unit/PanelWindowHookTests.cpp`. Evidence: focused tests cover clean collection preserving children, destroyed-child collection still removing the correct widget after an unrelated external-widget unconsider operation, an already destroyed external widget moved into a new container marking the new container dirty, explicit remove/full clear/garbage collection clearing external widget parent pointers, constructed-destroyed widgets being collected, direct cross-container moves detaching from the old parent, automatic reparenting preserving internal/external ownership modes, and duplicate consider calls not duplicating ownership.
- [x] T034 [US4] Add parent-pointer safety coverage for unparented widgets in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: rebuilt Debug test first failed on 2026-05-18 because `AWidget::m_parent` was not initialized to `nullptr`.

### Implementation

- [x] T030 [US4] Cache panel profiler scope names in `Runtime/UI/Panels/APanel.h`, `Runtime/UI/Panels/APanel.cpp`, `Runtime/UI/Panels/PanelWindow.h`, and `Runtime/UI/Panels/PanelWindow.cpp` so traces include names like `Panel::Draw:Hierarchy`.
- [x] T031 [US4] Cache TreeNode ImGui labels in `Runtime/UI/Widgets/Layout/TreeNode.h` and `Runtime/UI/Widgets/Layout/TreeNode.cpp`, rebuilding only when the node name or stable widget ID changes, and keep prefab presentation coloring as cached `useTextColor`/`textColor` state instead of a per-frame provider callback.
- [x] T032 [US4] Dirty-gate widget garbage collection in `Runtime/UI/Internal/WidgetContainer.h`, `Runtime/UI/Internal/WidgetContainer.cpp`, `Runtime/UI/Widgets/AWidget.h`, and `Runtime/UI/Widgets/AWidget.cpp`. Evidence: RED tests first failed on 2026-05-18 when a destroyed external widget was moved into a new container before collection, when removed/cleared/collected external widgets kept stale parent pointers, when `GetWidgets()` still exposed a mutable child vector, when `CreateWidget()` accepted a widget destroyed during construction without dirtying the container, when direct cross-container `ConsiderWidget()` left stale ownership behind, and when automatic reparenting changed internal/external ownership modes; `ConsiderWidget()` now marks the new container dirty if the widget is already destroyed, detaches from any previous parent while preserving its prior `EMemoryMode`, ignores duplicate same-container consideration, removal paths clear parent pointers before erasing or deleting widgets, `CreateWidget()` handles constructed-destroyed widgets, and public widget-list access is const-only.
- [x] T033 [US4] Run focused validation. Evidence: `cmake --build Build --target NullusUnitTests --config Debug` completed successfully on 2026-05-18 after the `m_parent = nullptr` fix, and focused tests passed `EditorRenderPathContractTests.WidgetContainersSkipPerFrameGarbageScansWhenClean` plus `PanelWindowHookTests.WidgetContainerGarbageCollectionRunsOnlyAfterDestroyMarksDirty`.

## Phase 7: User Story 5 - Updated Trace Identifies And Avoids Resource Resolution Stalls (Priority: P2)

**Goal**: Make the updated trace actionable at editor-panel depth, prevent trace-export duplication from obscuring frame timing, and remove generated-model resource-resolution behavior that turned cold material and mesh artifact misses into UI-frame stalls.

**Independent Test**: Profiler destination tests cover deep editor UI scopes and trace export cursor behavior; editor render-path contract tests cover deferred material binding continuing after unresolved cache-only material slots, fallback material creation avoiding synchronous default-material load, and deferred mesh artifact binding selecting the non-blocking upload path.

### Tests

- [x] T035 [US5] Add timeline depth coverage in `Tests/Unit/ProfilerDestinationTests.cpp`. Evidence: the test first failed on 2026-05-18 because depth 8 editor UI scopes incremented `GetSkippedScopeCountForTesting()` under the old timeline sink limit.
- [x] T036 [US5] Add trace-export cursor coverage in `Tests/Unit/ProfilerDestinationTests.cpp`. Evidence: the test first failed on 2026-05-18 because `ProfilerWindow.cpp` exported from the profiler frame range on every update without a `LastExportedFrame` cursor.
- [x] T037 [US5] Add missing-material-slot progression coverage in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: the test first failed on 2026-05-18 because `BindDeferredMaterialPaths()` had a `break;` after incrementing the material cache-miss counter, causing one missing cached material slot to consume one frame; later coverage was extended to require cache-only `GetResource(path, false)`, an unresolved-slot counter, no `PrewarmArtifact()` call, and a frame-budget yield inside the material-slot loop.
- [x] T043 [US5] Add fallback/default-material and deferred mesh upload contracts in `Tests/Unit/EditorRenderPathContractTests.cpp` and `Tests/Unit/MeshBoundingSphereTests.cpp`. Evidence: tests cover `GetOrCreateEditorDefaultMaterial()` using `EditorResources::GetLoadedShader("DebugLitColor")` instead of `MaterialManager::operator[](":Materials\\Default.mat")`, deferred generated mesh binding probing only already cached `ModelManager` resources and using `MeshBufferUploadMode::CpuToGpu` as an editor/RHI-owned cold fallback, background mesh artifact loading staying CPU-only, duplicate `.nmesh` references sharing a pending load and fallback transient model, precomputed artifact bounds being reused by mesh construction, `.nmesh` v2 header/bounds fields using stable little-endian byte order while vertex/index payloads remain native cache data, explicit zero-radius bounds, legacy v1 bounds computation, truncated v2 rejection, and default mesh construction preserving `MeshBufferUploadMode::GpuOnly`.
- [x] T045 [US5] Add bounded background-task and DX12 upload-heap state contracts in `Tests/Unit/EditorRenderPathContractTests.cpp`, `Tests/Unit/DX12TextureViewUtilsTests.cpp`, `Tests/Unit/UploadContextTests.cpp`, `Tests/Unit/GameObjectAssetImportTests.cpp`, and `Tests/Unit/ReflectionRuntimeEngineTests.cpp`. Evidence: tests first failed on 2026-05-19 because `EditorActions` retained raw per-task `std::thread` objects until destruction, `NativeDX12Buffer` initialized all upload-heap buffers as `ResourceState::ShaderRead`, default uniform upload-heap buffers still exposed `MemoryUsage::GPUOnly`, backend upload context could route CPU-visible destinations through copy/barrier upload work, DX12 textures accepted CPU-visible memory usage, async drag/drop scheduling rejection left a pending completion unresolved, async completion carried a stale parent pointer across frames, and `MeshRenderer` reflection clone could copy transient/event/failure-cache state.
- [x] T047 [US5] Add shader-cache-only material prewarm, prefab lookup index, and async import exception contracts in `Tests/Unit/AssetMaterialConversionTests.cpp` and `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: `MaterialLoaderCanKeepShaderResolutionCacheOnly` covers `{false, false}` shader cache-only behavior; `MaterialArtifactPrewarmDoesNotSynchronouslyLoadShaderDependencies` covers `MaterialManager::PrewarmArtifact()` using `{false, false}` and rejecting invalid prewarm results; `GeneratedModelMaterialResolutionKeepsColdMissesCacheOnly` covers invalid cached materials remaining unresolved before `SetMaterialAtIndex`; `GeneratedModelResourceResolutionIndexesAssetsAndLiveObjectsOncePerStep` first failed on 2026-05-19 because live source-object pointers were cached in `RendererResourceResolutionState`, `RunRendererResourceResolutionStep()` did not rebuild a current live index, and `FindPrefabInstanceObjectBySourceId()` still existed; `AsyncModelDropsScheduleImportAndCompletionCallbacks` first failed because `SaveAndReimport()` exceptions crossed the worker boundary before completion and later required non-pending completion without capturing the external progress tracker.
- [x] T049 [US5] Add generated-model main-package cache-miss regression coverage in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: `GeneratedModelDropTreatsMainModelPackageAsCacheWarmupOnly` failed when `ProbeMainModelPackageCache()` returned `modelPackage != nullptr`, which reproduced the dropped-model invisible regression by allowing a cold `.nmodel` cache miss to block downstream `.nmesh` binding tasks.

### Implementation

- [x] T038 [US5] Increase timeline CPU scope depth in `Runtime/UI/Profiling/TimelineProfilerSink.cpp` from 6 to 16 so `Canvas -> Panel -> Hierarchy` style scopes are retained.
- [x] T039 [US5] Track and update `LastExportedFrame` in `Runtime/UI/ImGuiExtensions/TimelineProfiler/ProfilerWindow.cpp` so trace export appends only frames not previously written, while seeding the cursor to the latest completed frame on Begin Trace so recording does not backfill old timeline history.
- [x] T040 [US5] Remove the per-missing-slot `break;` in `Project/Editor/Core/EditorActions.cpp` so deferred material binding scans bounded material-slot batches while still using non-loading cached material lookup, visible fallback material, unresolved-slot accounting, and a frame-budget yield.
- [x] T044 [US5] Remove synchronous fallback/default-material loading and mitigate deferred mesh bind upload stalls in `Project/Editor/Core/EditorActions.cpp`, `Project/Editor/Core/EditorResources.h`, `Project/Editor/Core/EditorResources.cpp`, `Runtime/Engine/Components/MeshRenderer.h`, `Runtime/Engine/Components/MeshRenderer.cpp`, `Runtime/Rendering/Assets/MeshArtifact.h`, `Runtime/Rendering/Assets/MeshArtifact.cpp`, `Runtime/Rendering/Resources/Mesh.h`, `Runtime/Rendering/Resources/Mesh.cpp`, `Runtime/Rendering/Resources/Loaders/ModelLoader.cpp`, `Runtime/Core/ResourceManagement/ModelManager.cpp`, `Runtime/Rendering/Buffers/VertexBuffer.h`, `Runtime/Rendering/Buffers/VertexBuffer.inl`, `Runtime/Rendering/Buffers/IndexBuffer.h`, and `Runtime/Rendering/Buffers/IndexBuffer.cpp`. Evidence: fallback uses already loaded `DebugLitColor`, generated mesh artifact background work only loads `.nmesh` CPU data, pending loads are deduped by model path, completed binds reuse only already cached `ModelManager` models and do not call synchronous `ModelManager::operator[]`, fallback transient CPU-to-GPU models are shared per pending load and not registered globally, `.nmesh` artifacts persist/read precomputed bounding spheres with stable field-wise header/bounds serialization, `BindDeferredMeshPath()` avoids recomputing bounds, `MeshRenderer` clone/copy does not copy transient model, stale event callbacks, or failed-load cache state, and normal mesh construction defaults to `GpuOnly`.
- [x] T046 [US5] Bound editor background work and fix DX12 CPU-to-GPU buffer state in `Project/Editor/Core/EditorActions.h`, `Project/Editor/Core/EditorActions.cpp`, `Project/Editor/Panels/AssetBrowser.cpp`, `Project/Editor/Panels/AssetProperties.cpp`, `Project/Editor/Assets/EditorAssetDragDropBridge.h`, `Project/Editor/Assets/EditorAssetDragDropBridge.cpp`, `Runtime/Rendering/RHI/Core/RHIEnums.h`, `Runtime/Rendering/RHI/Utils/UploadContext/UploadContext.cpp`, `Runtime/Rendering/RHI/Utils/ResourceStateTracker/ResourceStateTracker.cpp`, `Runtime/Rendering/RHI/Backends/DX12/DX12Command.cpp`, `Runtime/Rendering/RHI/Backends/DX12/DX12ExplicitDeviceFactory.cpp`, and `Runtime/Rendering/RHI/Backends/DX12/DX12Resource.cpp`. Evidence: `TrackBackgroundTask()` now queues callable work on a small reusable worker set with exception logging and shutdown cancellation for not-yet-started tasks, drag/drop async scheduling consumes rejection and completes with diagnostics, async pending-drop completion revalidates parent objects from the live scene before use, renderer resource tasks use deque pop/push instead of vector middle erase/insert, DX12 upload heaps report `ResourceState::GenericRead`, uniform upload heaps expose CPU-to-GPU effective memory usage, only legal fixed-state CPU-visible buffer barriers with legal before/after states are skipped, illegal CPU-visible barrier/copy requests are rejected/logged, CPU-to-GPU storage buffers and CPU-visible textures are rejected, and backend upload context rejects CPU-visible upload destinations.
- [x] T048 [US5] Keep material artifact prewarm dependency-cache-only, index generated-prefab asset resolution, rebuild live source-object lookup per resolution step, complete async import exceptions, and keep profiler trace recording observable in `Runtime/Rendering/Resources/Loaders/MaterialLoader.h`, `Runtime/Rendering/Resources/Loaders/MaterialLoader.cpp`, `Runtime/Core/ResourceManagement/MaterialManager.cpp`, `Project/Editor/Core/EditorActions.cpp`, `Project/Editor/Assets/EditorAssetDragDropBridge.cpp`, `Runtime/UI/ImGuiExtensions/TimelineProfiler/ProfilerWindow.cpp`, and `Project/Editor/Panels/ProfilerPanel.cpp`. Evidence: `MaterialLoader::LoadOptions` now separates missing texture and shader loading, `MaterialManager::PrewarmArtifact()` uses `{false, false}` and destroys invalid prewarm objects, `BindDeferredMaterialPaths()` treats invalid cached materials as unresolved, `HasResolvedMaterialBindings()` also rejects invalid cached materials before treating a generated model as already resolved, `CollectPrefabAssetResolutionTasks()` builds `PrefabResolvedAssetIndex`, `RunRendererResourceResolutionStep()` builds `BuildPrefabInstanceObjectIndex(*liveInstance)` once per current live instance and no longer stores `liveObjectsBySourceId` in delayed state, the async import lambda catches `std::exception`/unknown exceptions before invoking completion with `dragdrop-background-import-failed` and `pendingImport=false`, trace recording continues drawing the profiler timeline, Begin Trace does not export historical frames before the click, and `ProfilerPanel::DrawTimeline` keeps a dedicated attribution scope.
- [x] T050 [US5] Treat generated-model main `.nmodel` package probing as optional cache warmup in `Project/Editor/Core/EditorActions.cpp`. Evidence: `ProbeMainModelPackageCache()` still uses `modelManager.GetResource(task.modelPackagePath, false)` and increments `cachedModelPackages` on a warm hit, but now returns `true` on cold miss so per-mesh `.nmesh` background loads and transient CPU-to-GPU fallback binds continue instead of failing the whole renderer-resource resolution job.
- [x] T041 [US5] Run focused validation for profiler/UI/resource-resolution contracts and record the commands. Evidence: `cmd.exe /s /c "call ""F:\Microsoft Visual Studio\2022\VC\Auxiliary\Build\vcvars64.bat"" >nul && ""F:\Microsoft Visual Studio\2022\MSBuild\Current\Bin\amd64\MSBuild.exe"" Build\Tests\Unit\NullusUnitTests.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m"` passed on 2026-05-19 after material shader-cache-only, indexed prefab lookup, async import exception changes, and invalid cached material handling; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=EditorRenderPathContractTests.GeneratedModelMaterialResolutionKeepsColdMissesCacheOnly:EditorRenderPathContractTests.MaterialArtifactPrewarmDoesNotSynchronouslyLoadShaderDependencies --gtest_brief=1` passed 2 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=AssetMaterialConversionTests.MaterialLoaderCanKeepShaderResolutionCacheOnly:AssetMaterialConversionTests.MaterialPrewarmDoesNotPoisonLaterShaderLoading:EditorRenderPathContractTests.GeneratedModelMaterialResolutionKeepsColdMissesCacheOnly:EditorRenderPathContractTests.MaterialArtifactPrewarmDoesNotSynchronouslyLoadShaderDependencies:EditorRenderPathContractTests.AsyncModelDropsScheduleImportAndCompletionCallbacks:EditorRenderPathContractTests.GeneratedModelResourceResolutionIndexesAssetsAndLiveObjectsOncePerStep:EditorRenderPathContractTests.GeneratedModelDeferredMeshBindUsesNonBlockingUploadHeap:EditorRenderPathContractTests.GeneratedModelDeferredMeshLoadStaysCpuOnlyUntilUiThreadBind:UploadContextTests.BackendUploadContextRejectsCpuVisibleBufferDestinations:UploadContextTests.ResourceStateTrackerIgnoresOnlyLegalCpuVisibleBufferStates --gtest_brief=1` passed 10 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ProfilerDestinationTest.*:PanelWindowHookTests.ProfilerPanel*:PanelWindowHookTests.PanelDrawProfilerScopeUsesVisiblePanelName --gtest_brief=1` passed 25 tests.
- [ ] T042 [US5] Complete the plan-review quality gate.

## Phase 8: User Story 6 - Scene View Avoids Per-Frame Thread Drain Stalls (Priority: P1)

**Goal**: Keep ordinary Scene View navigation and inspection from forcing `AView::DrainThreadedRendering` every frame while preserving explicit resize/readback drains.

**Independent Test**: Panel lifecycle contract tests prove Scene View defaults to asynchronous retired-frame presentation, camera/gizmo/pending-pick hints do not force synchronization, and immediate readback plus resize drain cases remain covered.

### Tests

- [x] T051 [US6] Add Scene View asynchronous retired-frame presentation policy coverage in `Tests/Unit/PanelWindowHookTests.cpp`. Evidence: rebuilt Debug test first failed on 2026-05-19 because `ShouldSceneViewSynchronizeRetiredFramePresentation()` returned true and ordinary Scene View interaction hints still fed `ShouldDrainAfterRetirementAwareViewRender(..., requiresSynchronizedPresentation=true)`.

### Implementation

- [x] T052 [US6] Change Scene View synchronization policy in `Project/Editor/Panels/ViewFrameLifecycle.h` and `Project/Editor/Panels/SceneView.cpp` so ordinary presentation does not request a threaded-rendering drain. Evidence: Scene View default retired-frame presentation synchronization now returns false, and `SceneView::RequiresSynchronizedRetiredFramePresentation()` delegates only to that explicit policy instead of forcing drains for camera motion, camera control, gizmo activity, click-picking hints, or delayed picks.
- [x] T053 [US6] Run focused validation for Scene View lifecycle contracts and record the commands. Evidence: `cmd.exe /s /c "call ""F:\Microsoft Visual Studio\2022\VC\Auxiliary\Build\vcvars64.bat"" >nul && ""F:\Microsoft Visual Studio\2022\MSBuild\Current\Bin\amd64\MSBuild.exe"" Build\Tests\Unit\NullusUnitTests.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m"` passed on 2026-05-19; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=PanelWindowHookTests.SceneViewDoesNotSynchronizeRetiredFramePresentationByDefault:PanelWindowHookTests.SceneViewInteractionHintsDoNotForceThreadedRenderingDrain:PanelWindowHookTests.SceneViewRetainsExplicitReadbackAndResizeDrains:PanelWindowHookTests.RetirementAwareRenderPolicyDrainsOnlyForExplicitSynchronizationNeeds:PanelWindowHookTests.SceneViewCameraMotionIsTrackedWithoutForcingSynchronizedPresentation --gtest_brief=1` passed 5 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=PanelWindowHookTests.RetirementAware*:PanelWindowHookTests.SceneView*:PanelWindowHookTests.PendingSceneClickPickWaitsForRequestedPickingFrame:PanelWindowHookTests.RetirementAwareViewsUseCurrentOverlayMatrices:PanelWindowHookTests.SceneMutatingViewportOverlayRunsBeforeViewRender --gtest_brief=1` passed 14 tests.
- [x] T054 [US6] Complete the plan-review quality gate. Evidence: plan-review R1/R2/deeper audit completed on 2026-05-19 for `Project/Editor/Panels/ViewFrameLifecycle.h`, `Project/Editor/Panels/SceneView.cpp`, `Tests/Unit/PanelWindowHookTests.cpp`, and the Phase 8 spec/task updates. Auto-fail checks found no P0/P1 conditions: the change removes ordinary Scene View synchronized presentation drains, leaves explicit immediate-readback and resize drains covered by `ShouldDrainAfterRetirementAwareViewRender()`, adds focused policy regression coverage, and aligns with the `rendering_layout.md` editor-frame guidance that cursor/selection-style interaction hints should not force full hot-path redraw/synchronization when no hard synchronization contract exists. Note: the local Codex environment does not expose Claude's `Task` tool referenced by `/plan-review`; the required review loop was run inline without spawning a custom replacement agent.

## Phase 9: User Story 7 - Retained Render Prep Hotspots Are Observable And Cached (Priority: P1)

**Goal**: Use UE4.27 retained-scene architecture as direction while landing a low-risk Nullus bridge: expose renderer preparation churn in FrameInfo, revision-gate deferred GBuffer material synchronization, and prevent public material mutation APIs from bypassing revision invalidation.

**Independent Test**: Renderer stats tests prove the counters, deferred renderer tests prove GBuffer sync is skipped until source material identity or revisions change, panel tests prove FrameInfo/panel duration display, and editor render-path contract tests prove Hierarchy node count plus revision-aware material edit paths.

### Tests

- [x] T055 [US7] Add render-preparation counter coverage in `Tests/Unit/RendererStatsTests.cpp`. Evidence: test first failed on 2026-05-19 because `FrameInfo` did not expose `parseSceneCallCount`, drawable counts, `gBufferMaterialSyncCount`, `renderBindingSetCreationCount`, or `renderSnapshotBufferCreationCount`, and `RendererStats` had no recorders for them.
- [x] T056 [US7] Add deferred GBuffer material revision-cache coverage in `Tests/Unit/DeferredSceneRendererMaterialCacheTests.cpp`. Evidence: test first failed on 2026-05-19 because cached GBuffer materials re-synced on repeated frames with unchanged source material state; deeper review added `ResyncsSharedRuntimeVariantWhenSourceMaterialIdentityChanges` on 2026-05-20 to cover two runtime materials with the same pass variant and revision numbers but different parameter values.
- [x] T057 [US7] Add panel/FrameInfo/hierarchy performance observability coverage in `Tests/Unit/PanelWindowHookTests.cpp` and `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: coverage first failed because `APanel` did not expose last draw duration, `FrameInfo` did not display render-prep counters or target panel draw time, and `Hierarchy` did not expose node count.
- [x] T058 [US7] Add revision-safe material mutation contract coverage in `Tests/Unit/EditorRenderPathContractTests.cpp`. Evidence: coverage first failed because `GetUniformsData()` and `GetParameterBlock()` exposed mutable public access that could bypass material parameter revisions, and texture deletion propagation could mutate uniform values directly.

### Implementation

- [x] T059 [US7] Add renderer-owned render-preparation counters in `Runtime/Rendering/Data/FrameInfo.h`, `Runtime/Rendering/Core/RendererStats.h`, `Runtime/Rendering/Core/RendererStats.cpp`, and `Runtime/Engine/Rendering/BaseSceneRenderer.cpp`.
- [x] T060 [US7] Count binding-set and snapshot-buffer creation in `Runtime/Rendering/Core/ABaseRenderer.h`, `Runtime/Rendering/Core/ABaseRenderer.cpp`, `Runtime/Rendering/Core/CompositeRenderer.cpp`, and `Runtime/Rendering/Resources/Material.cpp`.
- [x] T061 [US7] Revision-gate deferred GBuffer material synchronization in `Runtime/Engine/Rendering/DeferredSceneRenderer.h`, `Runtime/Engine/Rendering/DeferredSceneRenderer.cpp`, `Runtime/Rendering/Resources/Material.h`, `Runtime/Rendering/Resources/Material.cpp`, `Runtime/Rendering/Resources/MaterialParameterBlock.h`, and `Runtime/Rendering/Resources/MaterialParameterBlock.cpp`.
- [x] T062 [US7] Expose render-prep and panel-scale observability in `Project/Editor/Panels/FrameInfo.h`, `Project/Editor/Panels/FrameInfo.cpp`, `Project/Editor/Panels/FrameInfoRendererStats.cpp`, `Runtime/UI/Panels/APanel.h`, `Runtime/UI/Panels/APanel.cpp`, and `Project/Editor/Panels/Hierarchy.h`.
- [x] T063 [US7] Route material editor and texture deletion mutation through revision-aware setters in `Project/Editor/Panels/MaterialEditor.cpp`, `Project/Editor/Core/EditorActions.cpp`, `Runtime/Rendering/Resources/Material.h`, and `Runtime/Rendering/Resources/Material.cpp`.
- [x] T064 [US7] Record UE4.27 comparison and latest trace evidence in `specs/028-large-model-scene-performance/spec.md`, `specs/028-large-model-scene-performance/plan.md`, and this task list. Evidence: local UE4.27 refs checked on 2026-05-19 include `PrimitiveSceneInfo.h` (`StaticMeshCommandInfos`, `CacheMeshDrawCommands`, `RemoveCachedMeshDrawCommands`, `AddStaticMeshes`, `UpdateStaticMeshes`), `MeshPassProcessor.h` (`FMeshDrawCommand`, dynamic instancing helpers, `RHIUpdateUniformBuffer` comment), `SceneVisibility.cpp` (`PrimitiveVisibilityMap`, `ParallelFor` frustum cull), and `MeshDrawCommands.cpp` (sort/merge, dynamic instancing, `SubmitMeshDrawCommandsRange`). Latest trace `App/Win64_Debug_Runtime_Shared/trace.json` was 10.32 MB, modified `2026-05-19 22:57:42`; `AView::DrainThreadedRendering` had no matches, while `DeferredSceneRenderer::GetOrCreateGBufferMaterial` totaled about 25.27 s over 2,789 events, `Panel::Draw:Scene View` averaged about 192 ms over 27 calls, and `AView::RendererBeginFrame` averaged about 84 ms over 28 calls.
- [x] T065 [US7] Run focused validation for render-prep counters, GBuffer material cache, FrameInfo/panel observability, and material revision API contracts. Evidence: `cmd.exe /s /c "call ""F:\Microsoft Visual Studio\2022\VC\Auxiliary\Build\vcvars64.bat"" >nul && ""F:\Microsoft Visual Studio\2022\MSBuild\Current\Bin\amd64\MSBuild.exe"" Build\Tests\Unit\NullusUnitTests.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m"` completed with updated `Build\bin\Debug\NullusUnitTests.exe` timestamp `2026-05-20 01:53:00`; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DeferredSceneRendererMaterialCacheTests.ResyncsSharedRuntimeVariantWhenSourceMaterialIdentityChanges --gtest_brief=1` passed 1 test; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DeferredSceneRendererMaterialCacheTests.* --gtest_brief=1` passed 4 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RendererStatsTests.RendererStatsTracksRenderPreparationCounters:DeferredSceneRendererMaterialCacheTests.*:PanelWindowHookTests.PanelDrawRecordsLastDrawDurationForFrameInfo:PanelWindowHookTests.FrameInfoPanelReadsRendererOwnedStats:EditorRenderPathContractTests.UiPanelAndHierarchyDrawPathsExposeActionablePerformanceScopes:EditorRenderPathContractTests.MaterialEditorUniformWidgetsUpdateMaterialThroughSetters:RenderFrameworkContractTests.DeferredThreadedGBufferCaptureUsesPassBindingPlaceholder --gtest_brief=1` passed 10 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DeferredSceneRendererMaterialCacheTests.*:RendererStatsTests.*:PanelWindowHookTests.FrameInfo*:PanelWindowHookTests.PanelDrawRecordsLastDrawDurationForFrameInfo:EditorRenderPathContractTests.UiPanelAndHierarchyDrawPathsExposeActionablePerformanceScopes:EditorRenderPathContractTests.MaterialEditorUniformWidgetsUpdateMaterialThroughSetters:RenderFrameworkContractTests.* --gtest_brief=1` passed 64 tests. The broader run emitted expected NONE-backend and telemetry error-path logs but exited successfully.
- [x] T066 [US7] Complete the plan-review quality gate. Evidence: inline plan-review ran two passes plus deeper audit on 2026-05-20; `docs/REVIEW_PATTERNS.md` was absent; the environment lacked the Claude `Task` tool required by the skill's subagent harness, so the review was performed inline with that limitation documented. R1 found and fixed one correctness issue where a shared runtime material pass variant could skip or overwrite GBuffer sync when source material identity changed but revision numbers matched; the fix uses a stable atomic `Material` instance id for runtime cache identity plus revision stamps for in-place edits. After the fix, no remaining P0/P1 findings were identified. Remaining P2/trade-off notes: this is still a bridge before persistent `RenderScene/RenderPrimitive/CachedDrawCommand`, and source-text contract tests remain brittle by design.

## Phase 10: User Story 8 - Persistent RenderScene Foundation (Priority: P1)

**Goal**: Formally introduce UE-style persistent renderer scene state so stable mesh/material draw command inputs are cached on `RenderPrimitive` objects and visible frames gather queues from retained state instead of rebuilding every draw input from components.

**Independent Test**: Render-scene cache tests prove primitives and cached commands survive unchanged frames, command inputs rebuild on model/material changes, transform/user-matrix changes update visible object data without rebuilding command caches, and the existing renderer consumes retained visible queues with the same opaque/transparent/skybox counts.

### Tests

- [x] T067 [US8] Add retained primitive/cache reuse tests in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `StableSceneReusesPersistentPrimitivesAndCachedCommandsAcrossFrames` first failed before `RenderScene` existed, then passed after retained primitive storage and cached command stamps were added.
- [x] T068 [US8] Add cached command invalidation tests for material/model/override changes in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `MaterialStateChangeInvalidatesOnlyAffectedCachedCommand` covers material render-state revision invalidation without rebuilding stable primitives.
- [x] T069 [US8] Add transform/user-matrix object descriptor reuse tests in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `TransformAndUserMatrixUpdateVisibleObjectDescriptorWithoutRebuildingCommand` verifies object data updates visible descriptors while cached command build count stays unchanged.
- [x] T070 [US8] Add renderer integration contract coverage proving `BaseSceneRenderer::ParseScene()` gathers from persistent `RenderScene` state. Evidence: `EditorRenderPathContractTests.SceneDrawableCollectionUsesPersistentRenderSceneCache` verifies `BaseSceneRenderer` owns `RenderScene`, calls `Synchronize()` / `GatherVisibleCommands()`, and no longer locally loops model renderers in `ParseScene()`.

### Implementation

- [x] T071 [US8] Create `Runtime/Engine/Rendering/RenderScene.h` and `Runtime/Engine/Rendering/RenderScene.cpp` with `RenderScene`, `RenderPrimitive`, `CachedDrawCommand`, command input stamps, and visible queue output.
- [x] T072 [US8] Synchronize persistent primitives from `Scene::FastAccessComponents`, reusing primitive entries across frames and removing entries for destroyed/removed mesh renderers.
- [x] T073 [US8] Cache mesh/material draw command inputs per primitive mesh slot and rebuild only when stable command-input stamps change.
- [x] T074 [US8] Gather visible opaque/transparent/skybox queues from retained commands while updating current transform/user-matrix object descriptors per frame.
- [x] T075 [US8] Route `BaseSceneRenderer::ParseScene()` through renderer-owned `RenderScene` and preserve existing draw count/stat behavior.
- [x] T076 [US8] Run focused validation for retained scene tests and existing forward/deferred render contracts. Evidence: Debug `NullusUnitTests` build passed; focused retained-scene run passed `RenderSceneCacheTests.*`; combined render contract runs passed retained scene, deferred GBuffer capture, renderer stats, generated model, and debug lifecycle filters.
- [x] T077 [US8] Complete the plan-review quality gate. Evidence: Phase 10 retained scene foundation passed focused validation and was included in the later Phase 11 review loop; no Phase 10-specific P0/P1 remained after the retained scene and visibility review fixes.

## Phase 11: User Story 8 - Bitset And Parallel Visibility (Priority: P1)

**Goal**: Replace per-model visible mesh vector filtering with a bitset-backed primitive visibility pass that can partition primitive ranges in parallel.

**Independent Test**: Visibility tests compare serial and parallel bitset results for the same scene and camera and verify visible queues match the retained-scene foundation.

### Tests

- [x] T078 [US8] Add bitset visibility result tests for primitive and mesh visibility in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `BitsetVisibilityTracksPrimitiveAndMeshResults` covers primitive and mesh bit counts, and `MeshBitsetCanCullIndividualMeshesInsideVisiblePrimitive` covers `CULL_MESHES` with one visible primitive containing one visible and one culled mesh.
- [x] T079 [US8] Add serial-vs-parallel visibility equivalence tests in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `SerialAndParallelVisibilityProduceEquivalentQueues` compares serial and explicit parallel bitsets plus visible mesh queues for 192 retained primitives and asserts explicit `Parallel` mode uses the parallel range-merge path; `AutoVisibilityUsesParallelPathForLargeScenes` separately covers the 1024-primitive Auto threshold.

### Implementation

- [x] T080 [US8] Add bitset-backed visibility storage and visible command queue construction from set bits. Evidence: `RenderSceneVisibilitySnapshot` stores primitive and mesh bitsets, and `GatherVisibleCommands()` builds queues from those bits instead of directly filtering a mesh vector.
- [x] T081 [US8] Add range-partitioned parallel visibility evaluation with a serial fallback for small primitive counts. Evidence: `RenderSceneVisibilityMode::Auto` keeps the renderer hot path serial below the threshold, while explicit `Parallel` mode always partitions primitive ranges, uses compact range-local bitsets, and merges them into the full visibility snapshot.
- [x] T082 [US8] Run focused visibility validation and trace-counter sanity checks. Evidence: `cmake --build Build --target NullusUnitTests --config Debug -- /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m` passed on 2026-05-20; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderSceneCacheTests.* --gtest_brief=1` passed 7 tests; combined retained-scene/render-contract run passed 11 tests; `DeferredSceneRendererMaterialCacheTests.*:RenderFrameworkContractTests.*:RendererStatsTests.*` passed 58 tests with expected NONE-backend telemetry error-path logs.
- [x] T083 [US8] Complete the plan-review quality gate. Evidence: plan-review R1 found a P0 where `Auto` visibility could enter per-frame `std::async` and allocate full-scene bitsets per range plus P1 gaps for `CULL_MESHES` coverage and task status. Fixes kept renderer hot-path `Auto` serial, made explicit `Parallel` use compact range-local bitsets, added `MeshBitsetCanCullIndividualMeshesInsideVisiblePrimitive`, and updated task status. R2 found 0 P0/P1 with one P2 docs mismatch, which was fixed by updating the retained primitive count from 96 to 192. Final deeper audit scored 70/80 with 0 P0/P1; remaining P2 follow-ups are single-hardware-thread async-branch test strength and future parallel multi-mesh range-merge coverage.

## Phase 12: User Story 8 - Per-Frame Object Buffer (Priority: P1)

**Goal**: Move per-object constants to indexed per-frame storage so retained cached draw commands do not require one uniform snapshot/binding-set creation per visible object.

**Independent Test**: Binding/provider tests prove visible commands receive stable object indices and repeated draws write object constants into per-frame storage without creating per-draw object binding snapshots.

### Tests

- [x] T084 [US8] Add per-frame object data index tests in `Tests/Unit/RendererFrameObjectBindingTests.cpp`. Evidence: object-indexed binding tests cover contiguous object indices, indexed binding capture, object-index push constants, instanced object-index ranges, and mixed indexed/legacy/indexed draw binding restoration.
- [x] T085 [US8] Add snapshot/binding creation regression tests for repeated visible object draws in `Tests/Unit/RendererStatsTests.cpp`. Evidence: retained object-buffer validation ran with `RendererStatsTests.*`, `RenderFrameworkContractTests.*`, and object-binding filters; repeated indexed draws reuse `EngineObjectDataBuffer`/structured binding set instead of creating per-draw object uniform snapshots.

### Implementation

- [x] T086 [US8] Add per-frame object constant storage and object-index assignment to retained visible commands. Evidence: `RenderScene::AssignVisibleObjectIndices()` assigns stable ranges after queue finalization, `DrawableObjectDescriptor` carries `objectIndex`/`objectCount`/`instanceModelMatrices`, and `ABaseRenderer` records object-index push constants for indexed draws.
- [x] T087 [US8] Update `EngineFrameObjectBindingProvider` and draw capture to bind per-frame object storage once and use object indices instead of per-draw object snapshots where supported. Evidence: provider uploads `EngineObjectDataBuffer`, creates an `ObjectData` structured-buffer binding set, preserves a separate cached structured binding set across legacy `ObjectConstants` draws, and registered engine shaders consume `ObjectData[u_ObjectIndex + SV_InstanceID]`.
- [x] T088 [US8] Run focused object-buffer and render-prep counter validation. Evidence: `NLS_Engine.vcxproj` Debug rebuilt successfully on 2026-05-20 after the material-owned GPU-instance object-data range fix; selected source compile passed for `RenderScene.cpp`, `EngineFrameObjectBindingProvider.cpp`, `RenderSceneCacheTests.cpp`, and `RendererFrameObjectBindingTests.cpp`. Fresh focused runs passed `RenderSceneCacheTests.*:RendererFrameObjectBindingTests.*` (37 tests), including `RetainedSingleDrawPreservesMaterialGpuInstances` and `EngineProviderUploadsMaterialOwnedGpuInstanceRangeForIndexedDraw`, and passed `RendererStatsTests.*:DeferredSceneRendererMaterialCacheTests.*:RenderFrameworkContractTests.*:EditorRenderPathContractTests.SceneDrawableCollectionUsesPersistentRenderSceneCache:EditorRenderPathContractTests.RegisteredObjectDataShadersUseInstanceIdOffset:EditorRenderPathContractTests.SceneDrawableCollectionAvoidsNodeAllocatedMultimapSort` (62 tests). A full `cmake --build Build --target NullusUnitTests --config Debug -- /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m` retry on 2026-05-20 timed out after 304 s while MSBuild/cl continued in the background, then naturally finished and relinked `Build\bin\Debug\NullusUnitTests.exe` at `2026/5/20 16:30:24`; the freshly linked binary passed `RendererStatsTests.IndexedObjectDrawsCreateOneObjectBindingSetForRepeatedVisibleDraws` after the prepared object-data slot was moved from draw scope to frame scope, passed the 9-test object-buffer regression filter covering explicit-frame slot isolation, strict indexed-shader rejection, immediate object-index push, indexed captures, instanced ranges, material-owned GPU instance ranges, and no-active-explicit fresh prepared buffers, then passed the broader retained-render/object-binding/stats/GBuffer/render-contract filter with 105 tests. Follow-up review validation on the freshly rebuilt 2026-05-20 binary passed the 11-test object-buffer filter including bounded no-active-explicit prepared-slot reuse and same-frame buffer-grow preservation. The broader run emitted expected NONE-backend and telemetry error-path logs but exited successfully.
- [x] T089 [US8] Complete the plan-review quality gate. Evidence: inline plan-review R1/R2/deeper audit ran on 2026-05-20 for Phase 12/13. `docs/REVIEW_PATTERNS.md` is absent, and this Codex environment does not expose Claude's `Task` harness required by the skill, so review was performed inline with that limitation documented. R1 found and fixed the indexed/legacy object binding-set state split plus single-object descriptor fallback; R2 found and fixed retained single draws incorrectly overriding material-owned GPU instancing by forcing `Drawable::instanceCount=1`; R3 found and fixed the remaining material-owned GPU-instancing object-data range hole where `SV_InstanceID > 0` could read outside the assigned object range after shaders moved to `ObjectData[u_ObjectIndex + SV_InstanceID]`. Follow-up multi-agent audit found and fixed prepared object slots growing without bound when no explicit frame was active, indexed-object shaders falling back to legacy `ObjectConstants` when `objectIndex` was invalid, `RenderSceneVisibilityMode::Auto` never selecting the parallel path for large scenes, and MetaParser accepting external reflection type macros from comments or `#if 0` blocks. The fixes bound prepared object data slots to the threaded/explicit frame slot count, make indexed-object shader draws fail closed when object data cannot be prepared, enable Auto to use range-partitioned visibility at the existing threshold, and sanitize comments/disabled blocks before external type discovery. The latest focused and broader validations found 0 remaining P0/P1 in the retained render/object-buffer/sort-merge changes. Remaining P2/trade-offs: backend validation remains DX12-aligned contract coverage rather than a RenderDoc capture, and this is a UE4.27-inspired staged bridge rather than a full RHI-level `FMeshDrawCommand` implementation.

## Phase 13: User Story 8 - Command Sort/Merge And Dynamic Instancing (Priority: P2)

**Goal**: Finalize visible cached command queues with UE-style state-aware sorting, compatible command merge, and dynamic instancing.

**Independent Test**: Command queue tests prove opaque sorting groups compatible state while transparent ordering remains back-to-front, and dynamic instancing merges only compatible mesh/material/pipeline commands with correct object-index ranges.

### Tests

- [x] T090 [US8] Add opaque sort and transparent ordering tests in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `OpaqueQueueGroupsCompatibleStateAndTransparentKeepsBackToFront` covers compatible opaque grouping/merge while transparent draws remain distance-descending.
- [x] T091 [US8] Add dynamic instancing compatibility and rejection tests in `Tests/Unit/RenderSceneCacheTests.cpp`. Evidence: `DynamicInstancingMergesCompatibleOpaqueCommandsIntoObjectIndexRange` and `DynamicInstancingRejectsIncompatibleMeshMaterialAndTransparentCommands` cover merge ranges, per-instance matrices, material/mesh incompatibility, and transparent rejection.

### Implementation

- [x] T092 [US8] Add cached command sort keys and opaque state-aware queue sorting. Evidence: `RenderScene::FinalizeOpaqueQueue()` uses material instance id/address, mesh address, state mask, primitive mode, and distance sort keys, so opaque queues now pay one final state-aware stable sort instead of a distance pre-sort plus a second state sort; transparent queues still use back-to-front sorting and are not finalized through the opaque merge path.
- [x] T093 [US8] Add command merge metadata and dynamic instancing ranges with per-instance object indices. Evidence: compatible opaque commands are merged into `Drawable::instanceCount` ranges, descriptor `objectCount`, and `instanceModelMatrices`, with merge guards for material/mesh/state/primitive compatibility, non-blendable material, parameter-struct shader support, and valid descriptor range metadata.
- [x] T094 [US8] Run focused sort/merge/instancing validation plus forward/deferred render contract tests. Evidence: after the final material-owned GPU-instance object-data range fix, `NLS_Engine.vcxproj` Debug rebuilt successfully; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderSceneCacheTests.*:RendererFrameObjectBindingTests.* --gtest_brief=1` passed 37 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RendererStatsTests.*:DeferredSceneRendererMaterialCacheTests.*:RenderFrameworkContractTests.*:EditorRenderPathContractTests.SceneDrawableCollectionAvoidsNodeAllocatedMultimapSort:EditorRenderPathContractTests.SceneDrawableCollectionUsesPersistentRenderSceneCache:EditorRenderPathContractTests.RegisteredObjectDataShadersUseInstanceIdOffset --gtest_brief=1` passed 62 tests. A fresh 2026-05-20 validation on the `16:30:24` `NullusUnitTests.exe` binary passed the 9-test object-buffer regression filter and the broader 105-test retained-render/object-binding/stats/GBuffer/render-contract filter. Follow-up validation after removing the redundant opaque pre-sort rebuilt `NullusUnitTests` Debug and passed `EditorRenderPathContractTests.SceneDrawableCollectionAvoidsNodeAllocatedMultimapSort` plus the three sort/merge/dynamic-instancing `RenderSceneCacheTests`, the 11-test object-buffer filter, the broader 108-test retained-render/object-binding/stats/GBuffer/render-contract filter, and the 3-test MetaParser focused filter.
- [x] T095 [US8] Complete the plan-review quality gate. Evidence: Phase 13 was included in the 2026-05-20 inline and multi-agent review loop described in T089. R1/R2/R3 findings were fixed: object data binding sets are cached separately from legacy object constants, indexed single-object descriptors fall back to `objectCount=1`, opaque sort avoids direct raw-pointer `<`, dynamic-instancing descriptors are range-validated before merge, retained single draws preserve material-owned GPU instance counts unless the queue actually merges compatible commands, and material-owned GPU instancing now receives a matching object-data range for every `SV_InstanceID`. Follow-up validation fixed the remaining frame-slot/binding churn regression by keeping no-active-explicit object-data slots frame-local instead of draw-local, then bounded those slots by frame-slot count after the multi-agent audit. Final follow-up also removed the redundant opaque pre-sort so opaque finalization does one state-aware stable sort and merge pass. Latest deeper audit found 0 P0/P1; remaining P2 is that Nullus still has a staged retained-command bridge, not UE4.27's complete render-thread `FMeshDrawCommand`/GPUScene stack.

## Phase 14: User Story 8 - Follow-Up Review Hardening (Priority: P1)

**Goal**: Close review-discovered correctness gaps in the retained command/object-data bridge before treating the UE-style render-prep increment as complete.

**Independent Test**: Focused object-binding and MetaParser generation tests prove indexed-object shaders fail closed when object descriptor data is missing, prepared object-data slots reserve the actual frame slot they will publish into, generated sources expose runtime meta properties before reflected type metadata uses them, and the latest generated/unit-test binary rebuilds cleanly.

### Tests

- [x] T096 [US8] Add indexed-shader missing-object-descriptor regression coverage in `Tests/Unit/RendererFrameObjectBindingTests.cpp`. Evidence: `EngineProviderRejectsIndexedShaderWhenObjectDescriptorIsMissing` covers an indexed-object shader draw with no `DrawableObjectDescriptor` and requires `PrepareDraw()` / prepared binding capture to fail without producing a legacy `ObjectConstants` binding set.
- [x] T097 [US8] Add MetaParser source-template include-order coverage in `Tests/Unit/MetaParserGenerationModuleTests.cpp`. Evidence: `HeaderSourceTemplateIncludesRuntimeMetaPropertiesBeforeReflectedHeader` locks `Reflection/RuntimeMetaProperties.h` before the reflected header include so generated component sources can instantiate type metadata such as `ComponentMenu("Rendering/Camera")` after the runtime meta-property generated body is visible.
- [x] T098 [US8] Add prepared frame-slot reservation lifecycle coverage in `Tests/Unit/ThreadedRenderingLifecycleTests.cpp` and prepared object-buffer provider coverage in `Tests/Unit/RendererFrameObjectBindingTests.cpp`. Evidence: lifecycle tests cover reserved reusable slots being skipped by snapshot publication and consumed only by prepared publication, while provider tests cover stale reservation release, bounded no-active-explicit prepared slots, retired-slot reuse, and same-frame object-buffer growth preservation.

### Implementation

- [x] T099 [US8] Replace no-active-explicit object-data slot peeking with real threaded frame-slot reservation in `Runtime/Rendering/Context/ThreadedRenderingLifecycle.{h,cpp}`, `Runtime/Rendering/Context/DriverAccess.h`, `Runtime/Rendering/Context/Driver.cpp`, and `Runtime/Engine/Rendering/EngineFrameObjectBindingProvider.cpp`. Evidence: `ReserveReusableSlotIndex()` / `ReleaseReservedReusableSlotIndex()` / `GetReservedReusableSlotIndex()` reserve a reusable lifecycle slot; snapshot publication skips reserved slots; prepared publication consumes the reserved slot; `DriverRendererAccess::GetActiveDescriptorAllocator()` and `CreateExplicitBindingSet()` use the reserved slot allocator/frame index when no explicit frame is active; stale provider reservations are released on the next `OnBeginFrame()`.
- [x] T100 [US8] Keep indexed-object shader draws fail-closed for invalid or missing object data in `Runtime/Engine/Rendering/EngineFrameObjectBindingProvider.cpp`. Evidence: indexed-object shaders no longer fall back to legacy `ObjectConstants` when object index range preparation fails or when the drawable has no object descriptor; `EngineProviderRejectsSparseExternalObjectIndexForIndexedDraw`, `EngineProviderRejectsIndexedShaderWhenObjectDataRangeCannotBePrepared`, `EngineProviderRejectsIndexedShaderWhenObjectDescriptorIsMissing`, and `ImmediateIndexedShaderDrawIsSkippedWhenObjectDataRangeCannotBePrepared` cover these paths.
- [x] T101 [US8] Finalize low-risk render-prep hardening in `Runtime/Engine/Rendering/RenderScene.cpp`, `Runtime/Rendering/Data/DrawableInstanceCount.h`, and `Tools/MetaParser/src/Templates/HeaderGenerated.cpp.tt`. Evidence: opaque dynamic-instancing merge now performs one state-aware stable sort followed by a linear compatible-run merge; shared instance-count resolution lives in `DrawableInstanceCount.h`; `RenderSceneVisibilityMode::Auto` selects the parallel bitset path at the 1024-primitive threshold; MetaParser generated sources include `RuntimeMetaProperties.h` before reflected headers and use `type.HasGeneratedBody` rather than the old `GeneratedBodyLine` template shape.
- [x] T102 [US8] Run final focused validation for the follow-up fixes. Evidence: `cmake --build Build --target NullusUnitTests --config Debug -- /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m` rebuilt successfully on 2026-05-20 and produced `Build\bin\Debug\NullusUnitTests.exe` at `2026/5/20 20:11:23`; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RendererFrameObjectBindingTests.EngineProviderRejectsIndexedShaderWhenObjectDescriptorIsMissing:RendererFrameObjectBindingTests.EngineProviderRejectsIndexedShaderWhenObjectDataRangeCannotBePrepared:RendererFrameObjectBindingTests.EngineProviderRejectsSparseExternalObjectIndexForIndexedDraw:RendererFrameObjectBindingTests.ImmediateIndexedShaderDrawIsSkippedWhenObjectDataRangeCannotBePrepared --gtest_brief=1` passed 4 tests; `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=MetaParserGenerationModuleTests.HeaderSourceTemplateIncludesRuntimeMetaPropertiesBeforeReflectedHeader:MetaParserGenerationModuleTests.HeaderSourceTemplateUsesGeneratedTypeTemplateModelShape:MetaParserGenerationModuleTests.ExternalReflectionTypeDiscoveryHandlesCommentsStringsAndElifBranches:MetaParserGenerationModuleTests.MapsDenseRuntimeMetaPropertyGeneratedBodiesToTheirOwnTypes --gtest_brief=1` passed 4 tests. The object-binding focused run emitted expected NONE-backend RenderDoc/telemetry warnings and exited successfully.
  Additional evidence: a fresh `cmake --build Build --target NullusUnitTests --config Debug -- /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m` rebuild produced `Build\bin\Debug\NullusUnitTests.exe` at `2026/5/20 21:14:30`; focused runs passed 6 new hardening tests for reflection-only indexed shaders, shared object-data range limits, pre-allocation invariant failure, unsupported Vulkan indexed-object pipeline-layout rejection, explicit parallel visibility, and Auto-threshold visibility; broader validation passed the 57-test retained-scene/object-binding/lifecycle filter, `MetaParserGenerationModuleTests.*` (12 tests), and the 62-test renderer stats/GBuffer/render-contract filter.
- [x] T103 [US8] Complete the final plan-review / multi-agent quality gate after the follow-up fixes. Evidence: final review used `/plan-review` criteria plus existing multi-agent audit outputs, then re-checked current code against stale findings. Current code has the follow-up P0/P1 fixes in place: shared `DrawableInstanceCount.h`, shared `ObjectDataLimits.h`, reflection-aware indexed-object shader support, fail-closed unsupported Vulkan indexed-object layout, lifecycle-reserved prepared object-data slots, backpressure failure without object-buffer overwrite, idle high-water object-buffer reset, explicit `Parallel` visibility coverage, and MetaParser comment/string/#elif behavior coverage. Fresh validation on 2026-05-20 rebuilt `NullusUnitTests` Debug with `cmake --build Build --target NullusUnitTests --config Debug -- /m:1 /nr:false /p:TrackFileAccess=false /p:LinkIncremental=false /v:m` (exit 0, `Build\bin\Debug\NullusUnitTests.exe` timestamp `2026/5/20 21:35:01`), then passed the 9-test object-binding/lifecycle hardening filter, all 14 `RenderSceneCacheTests.*`, and the 74-test MetaParser/renderer-stats/GBuffer/render-contract filter. Remaining review items are P2/trade-offs only: the implementation is a UE4.27-inspired staged bridge rather than full `FMeshDrawCommand`/GPUScene parity, `std::async` visibility should eventually move to an engine job system, RenderDoc/GPU-backend capture was not run in this pass, and `docs/REVIEW_PATTERNS.md` was absent so known-pattern grep could not be performed.
