#include "Rendering/SceneRendererFactory.h"

// Kept as a physical translation unit so stale IDE/CMake project files that still list this
// source continue to compile cleanly after the factory implementation moved inline into the header.
