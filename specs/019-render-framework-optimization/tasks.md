# Tasks: Render Framework Optimization

**Input**: User-provided prioritized TODO list and design documents in `specs/019-render-framework-optimization/`
**Prerequisites**: [spec.md](spec.md), [plan.md](plan.md), [research.md](research.md)
**Tests**: Behavior changes use TDD where possible; completed items must record validation evidence.

## Status Legend

- `[ ]`: not started
- `[x]`: completed with evidence
- `[~]`: in progress or partially complete
- `[!]`: blocked

## P0 / Correctness

- [x] T001 [P0] Fix deferred threaded path not executing the deferred FrameGraph. Evidence: `ThreadedRenderingLifecycleTests.DeferredSceneRendererPublishesPreparedRenderScenePackageWhenThreadedWorkersArePaused` and `DeferredSceneRendererPublishesSnapshotOwnedSceneInputsWhenThreaded` passed in Debug targeted run on 2026-05-08; the retired threaded slot is renderer-prepared and contains deferred `GBuffer` then `Lighting` pass inputs.
- [x] T002 [P0] Unify the authoritative resource-state source between DX12 command buffers and `ResourceStateTracker`. Evidence: `FrameGraphSceneTargetsTests.VisibilityTransitionsPreferTrackerStateOverResourceReportedState` first failed when `RecordResourceVisibilityTransitions()` prefilled `before` from stale resource state instead of tracker state, then passed after letting `FrameGraphExecutionContext` resolve tracked state; `FrameGraphSceneTargetsTests.BeginPassAttachmentTransitionsPreferTrackerStateOverResourceReportedState` first failed because `BeginPassCommandPlan` had no tracker context/contract, then passed after pass attachment transitions moved to tracker-managed barriers and DX12 render passes can skip internal attachment state mutation; `FrameGraphSceneTargetsTests.EndSwapchainPassTransitionsTrackedBackbufferToPresent`, `FrameGraphSceneTargetsTests.FrameGraphExecutionContextCommitsTrackedBarriersOnlyWhenStateChanges`, `ThreadedRenderingLifecycleTests.TranslationMergeInsertsBarrierBatchForDeferredWorkUnits`, and `ThreadedRenderingLifecycleTests.DriverRhiWorkerTransitionsExtractedOffscreenTexturesToShaderRead` passed in Debug targeted run on 2026-05-08.
- [x] T003 [P0] Add deferred threaded/GBuffer barrier and visibility contract from GBuffer to Lighting. Evidence: `ThreadedRenderingLifecycleTests.TranslationMergeInsertsBarrierBatchForDeferredWorkUnits` passed in Debug targeted run on 2026-05-08 and verifies barrier command buffers between deferred GBuffer and Lighting work units.
- [x] T004 [P0] Give `MultiFramebuffer` a `Framebuffer`-style retired-resource delayed release mechanism. Evidence: `FrameGraphSceneTargetsTests.MultiFramebufferResizeRetainsPreviousExplicitResourcesTemporarily`, `MultiFramebufferFailedResizeCanRetrySameTargetSize`, and `MultiFramebufferZeroSizeResizeRetiresPreviousExplicitResources` passed in targeted and full unit test runs on 2026-05-08.
- [x] T005 [P0] Define swapchain resize GPU drain and resource lifetime contract before resize. Evidence: `DriverSwapchainResizeTests.ThreadedResizeWaitsForInFlightFrameRetirementBeforeResizingSwapchain`, `ThreadedResizeAppliesPendingResizeBeforeRecoveredSwapchainPublish`, and `RetriesPendingSwapchainResizeAfterBackendResizeFailure` passed in Debug targeted run on 2026-05-08.
- [x] T006 [P0] Fix `Mesh::ComputeBoundingSphere()` `numeric_limits<float>::min()` initialization bug. Evidence: `MeshBoundingSphereTests.ComputesCenterFromAllNegativeVertexPositions` passed in targeted and full unit test runs on 2026-05-08.
- [x] T007 [P0] Avoid using raw `Material*` as long-lived key in deferred GBuffer material cache. Evidence: `DeferredSceneRendererMaterialCacheTests.ReusesGBufferMaterialForStableMaterialAssetPath` first failed with cache size `2` for two material instances sharing the same asset path, then passed after using a stable material identity key; deferred threaded smoke tests `DeferredSceneRendererPublishesPreparedRenderScenePackageWhenThreadedWorkersArePaused` and `DeferredSceneRendererPublishesSnapshotOwnedSceneInputsWhenThreaded` also passed in Debug targeted run on 2026-05-08.
- [x] T049 [P0/P1] Make async DX12 readback polling finalize the copy/map path and clear `readbackInFlight` before reporting success. Evidence: `DX12ReadbackUtilsTests.PollingCompletedAsyncReadbackFinalizesAndAllowsNextReadback` passed on 2026-05-09 after `DX12ReadbackCompletionToken::GetStatus()/IsComplete()` were changed to finalize the map/copy/unmap path and clear the in-flight flag before reporting success; a second async readback can begin after polling completion.

## P1 / Resources And Synchronization

- [x] T008 [P1] Converge DX12 texture/buffer initial upload into unified `UploadContext`. Evidence: `DX12TextureUploadUtilsTests.BuildsInitialBufferUploadRequest` and `BuildsInitialTextureUploadRequestFromTexturePlan` first failed because the unified initial-upload request builders were missing, then passed after buffer and texture initial data were routed through shared DX12 initial-upload request/context entry points; `DX12TextureUploadUtilsTests.*`, `TextureResourceLifecycleTests.*`, `FrameGraphSceneTargetsTests.BeginPassAttachmentTransitionsPreferTrackerStateOverResourceReportedState`, and `FrameGraphSceneTargetsTests.EndSwapchainPassTransitionsTrackedBackbufferToPresent` passed in Debug targeted run on 2026-05-08. Remaining synchronous fence/completion behavior is tracked separately by T009/T010.
- [x] T009 [P1] Introduce async upload/readback API with fence handle or completion token. Evidence: added backend-neutral `RHICompletionToken`/`RHICompletionStatus`, `UploadSubmission` with completion token support on `UploadContext`, and `BeginReadPixels()` through `RHIDevice`, `RhiThreadCoordinator`, `DriverRendererAccess`, and `ABaseRenderer`; DX12 readback now returns a completion token that waits/maps/copies on completion while legacy `ReadPixelsChecked()` remains a synchronous wrapper over begin+wait. `UploadContextTests.SubmitUploadBufferReturnsCompletedTokenForAcceptedRequest`, `UploadContextTests.SubmitUploadBufferReturnsFailedTokenForRejectedRequest`, and `ThreadedRenderingLifecycleTests.DriverBeginReadPixelsReturnsCompletionTokenWithoutWaiting` passed after `cmake -S . -B Build` and Debug build on 2026-05-08.
- [x] T010 [P1] Avoid creating a command list, fence, event, and synchronous wait inside every `ReadPixels()` call. Evidence: `DX12ReadbackContext` now owns reusable command allocator/list, fence/event, and scratch readback buffer; `BuildDX12ReadbackScratchResourcePlan()` reuses the readback buffer until capacity is exceeded; `NativeDX12ExplicitDevice` keeps one readback context instead of allocating per call. The legacy synchronous call still waits for compatibility, but the wait is isolated in the completion token path and the new `BeginReadPixels()` API can return without waiting. `DX12ReadbackUtilsTests.PlansScratchReadbackResourceReuseUntilCapacityIsExceeded`, `DX12ReadbackUtilsTests.*`, and T019/T009 targeted readback tests passed in Debug targeted run on 2026-05-08; `git diff --check` passed with only CRLF warnings.
- [x] T011 [P1] Make readback use real resource state or FrameGraph tracker instead of assuming `COMMON`. Evidence: `DX12ReadbackUtilsTests.PreservesTrackedSourceStateAroundCopy` and `UnknownSourceStateFallsBackToCommonEquivalent` first failed because readback barrier-state planning was missing and DX12 readback hardcoded `COMMON -> COPY_SOURCE -> COMMON`; after adding readback barrier-state planning from the source texture state and restoring that state after copy, `DX12ReadbackUtilsTests.*` passed in Debug targeted run on 2026-05-08.
- [x] T012 [P1] Extend readback format support and return clear failure diagnostics. Evidence: `DX12ReadbackUtilsTests.ValidatesSupportedRgbAndRgbaUnsignedByteRequests` and `ReportsUnsupportedReadbackRequestReason` first failed because DX12 readback had no request validation/diagnostic path, then passed after adding `ValidateDX12ReadbackRequest()` with explicit support for RGB/BGR/RGBA/BGRA unsigned-byte destinations and reason strings for unsupported source formats or destination types; `DX12ReadbackUtilsTests.*` passed in Debug targeted run on 2026-05-08.
- [x] T013 [P1] Support texture in-place update and reduce the current recreate-on-data path. Evidence: `TextureResourceLifecycleTests.SameSizeTextureDataUpdateUsesInPlaceUploadInsteadOfRecreate` first failed because same-size texture data updates recreated the RHI texture and never called `UpdateTexture()`, then passed after adding `RHITextureUpdateDesc`/`RHIUpdateResult`, routing same-dimension/same-format updates through `RHIDevice::UpdateTexture()`, and adding a DX12 `UpdateNativeDX12Texture()` path that reuses the upload copy flow for full mip-0 updates. `TextureResourceUpdateUtilsTests.UsesInPlaceUpdateWhenInitialDataIsProvidedAndDescriptorMatches`, `TextureResourceLifecycleTests.*`, and `TextureResourceUpdateUtilsTests.*` passed in Debug targeted run on 2026-05-08; `git diff --check` passed with only CRLF warnings.
- [x] T014 [P1] Define cross-frame keepalive strategy for transient, offscreen, and FrameGraph resources. Evidence: `FrameGraphSceneTargetsTests.FrameGraphCreatedResourcesRegisterAndRetireTransientLifetime` first failed because `FrameGraphTexture::destroy()` released the transient texture view before tracker retirement, then passed after adding transient texture-view registration/retirement to `ResourceStateTracker` and registering FrameGraph-created views for the same frame lifetime; `ThreadedRenderingLifecycleTests.DriverRhiWorkerTransitionsExtractedOffscreenTexturesToShaderRead`, `ThreadedPreparedFramePublishesCompletedPreferredReadbackTexture`, `CompletedReadbackHistoryRetainsPreviousTextureForExplicitReadbackConsumers`, and external scene-output FrameGraph tests passed in Debug targeted run on 2026-05-08.
- [x] T015 [P1] Preserve existing texture resources when recreate/upload fails. Evidence: `TextureResourceLifecycleTests.FailedRecreateKeepsPreviousTextureAndView`, `FailedCreateFromMemoryUploadKeepsTexture2DMetadataAlignedWithFallbackTexture`, and `FailedTextureCubeUploadReportsFailureAndKeepsFallbackTexture` passed in targeted runs on 2026-05-08.
- [x] T016 [P1] Make cubemap loading/upload failures report failure instead of returning misleading fallback success. Evidence: `TextureResourceLifecycleTests.FailedTextureCubeUploadReportsFailureAndKeepsFallbackTexture` and `TextureResourceLifecycleTests.CreateCubeMapReturnsNullWhenSourceImagesCannotLoad` passed in Debug targeted run on 2026-05-08.
- [x] T050 [P1] Fix fence/readback timeout conversion so positive sub-millisecond nanosecond timeouts round up instead of becoming `0ms`. Evidence: `DX12ReadbackUtilsTests.PositiveNanosecondTimeoutsRoundUpToAtLeastOneMillisecond` and `ZeroNanosecondTimeoutMeansInfiniteWait` passed on 2026-05-09 after adding shared DX12 nanosecond-to-millisecond ceil conversion used by readback completion waits and `NativeDX12Fence::Wait()`.
- [x] T051 [P1] Make `NativeDX12Fence::Wait()` fail immediately when `SetEventOnCompletion()` fails instead of waiting on a misleading event. Evidence: `NativeDX12Fence::Wait()` now checks the `SetEventOnCompletion()` HRESULT and returns `false` after closing the event on failure; Debug `NullusUnitTests` build and `DX12ReadbackUtilsTests.*` passed on 2026-05-09.
- [x] T052 [P1] Replace UI bridge `INFINITE` waits for backbuffer reuse, submitted UI work, and shutdown drain with bounded waits and diagnostics. Evidence: `DX12UIFrameFenceTrackerTests.UiFenceWaitTimeoutIsFinite` passed on 2026-05-09; `DX12UIBridge` now routes backbuffer reuse, submitted UI work, and shutdown waits through a bounded 5s fence wait helper with `SetEventOnCompletion()`/timeout diagnostics.
- [x] T053 [P1] Clear UI composition signal when `EndStandaloneExplicitFrame(..., false)` skips present so stale UI fences cannot leak into the next present. Evidence: `ThreadedRenderingLifecycleTests.StandaloneExplicitFrameWithoutPresentClearsUiCompositionSignal` and `StandaloneExplicitFramePresentUsesUiCompositionBoundaryAndClearsSignal` passed on 2026-05-09 after standalone frame finalization clears the UI composition signal regardless of whether present occurs.
- [x] T059 [P1] Replace remaining DX12 initial-upload and shader-visible descriptor heap `INFINITE` waits with bounded fence waits and checked event/signal paths. Evidence: `RenderFrameworkContractTests.DX12UploadAndDescriptorWaitsUseBoundedFencePolicy`, `DX12ReadbackUtilsTests.*`, and `DX12UIFrameFenceTrackerTests.*` passed on 2026-05-09 after `DX12Resource.cpp` and `DX12Descriptor.cpp` routed these waits through bounded `WaitForDX12FenceValue()` helpers; `rg` found no `WaitForSingleObject(fenceEvent, INFINITE)` in those paths.

## P1 / RHI Architecture

- [x] T017 [P1] Reduce `void*` and native-handle leakage in the RHI layer. Evidence: UI present synchronization and core native RHI accessors now pass `NativeHandle` with backend tags instead of raw `void*` for command buffers, swapchains, fence/semaphore handles, UI bridge semaphores, and `RHIPresentDesc::uiSignalSemaphore`; `ThreadedRenderingLifecycleTests.ThreadedUiPresentSubmitsStandaloneExplicitFrameWithoutOnDemandAcquireBypass` first failed to compile when the test required a tagged UI signal semaphore, then passed after the API migration. Validation: `cmake --build Build --target NullusUnitTests --config Debug`, `ThreadedRenderingLifecycleTests.ThreadedUiPresentSubmitsStandaloneExplicitFrameWithoutOnDemandAcquireBypass`, `UIAndToolingBackendAwarenessTests.*`, `DriverSwapchainResizeTests.*`, and `FrameGraphSceneTargetsTests.FrameGraphCreatedResourcesRegisterAndRetireTransientLifetime` passed on 2026-05-08; `rg` found no remaining RHI native accessor signatures matching `virtual void* GetNative`, `void* GetNativeCommandBuffer`, `void* GetNativeSwapchainHandle`, `void* GetNativeFenceHandle`, or `void* GetNativeSemaphoreHandle`.
- [x] T018 [P1] Split UI bridge out of default empty `RHIDevice` implementation into explicit capability/interface. Evidence: `UIAndToolingBackendAwarenessTests.RhiDeviceBaseDoesNotExposeDefaultUiBridgeHooks` first failed because `RHIDevice` exposed default `PrepareUIRender()`, `ReleaseUITextureHandles()`, and `SetCurrentCommandBuffer()` hooks, then passed after introducing explicit `RHIUIDeviceBridge` capability access through `RHIDevice::GetUIBridgeDevice()` and routing Driver UI calls through the optional interface; `UIAndToolingBackendAwarenessTests.*`, `DriverNullDeviceFallbackTests.*`, and threaded UI frame-context tests passed in Debug targeted run.
- [x] T019 [P1] Change `ReadPixels()` from synchronous `void` API to status/error-returning API. Evidence: `DX12ReadbackUtilsTests.ValidateReadPixelsInputsReturnsStatusAndMessage` first failed before status-returning validation existed, then passed after adding `RHIReadbackResult`/`ReadPixelsChecked()` at the RHI device layer and mapping DX12 bottom-level readback status into RHI results; `ThreadedRenderingLifecycleTests.DriverReadPixelsCheckedPropagatesExplicitReadbackFailure` first failed because `DriverRendererAccess::ReadPixelsChecked()` did not exist, then passed after status propagation through `RhiThreadCoordinator`, `DriverRendererAccess`, and `ABaseRenderer`; `PickingRenderPass::PickAtRenderCoordinate()` now uses the checked API and returns no selection on readback failure with diagnostics. Validation: `cmake --build Build --target NullusUnitTests --config Debug`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DX12ReadbackUtilsTests.*:ThreadedRenderingLifecycleTests.CompletedReadbackHistoryRetainsPreviousTextureForExplicitReadbackConsumers:ThreadedRenderingLifecycleTests.DriverReadPixelsCheckedPropagatesExplicitReadbackFailure`, and `git diff --check` passed on 2026-05-08.
- [x] T020 [P1] Replace `CreateTexture/CreateBuffer(initialData)` with clearer upload descriptor semantics. Evidence: `DX12TextureUploadUtilsTests.BuildsInitialBufferUploadRequestFromExplicitUploadDesc` and `BuildsInitialTextureUploadRequestFromExplicitUploadDesc` first failed to compile because explicit upload descriptor types and helper overloads were missing, then passed after adding `RHIBufferUploadDesc`/`RHITextureUploadDesc`, moving `RHIDevice::CreateBuffer/CreateTexture` to descriptor-based upload overloads, and migrating buffer/texture call sites to pass explicit sizes; `cmake --build Build --target NullusUnitTests --config Debug`, `DX12TextureUploadUtilsTests.*`, `TextureResourceLifecycleTests.*:TextureResourceUpdateUtilsTests.*`, and targeted FrameGraph/renderer/threaded tests passed on 2026-05-09. `rg` found no remaining RHI resource creation calls using `nullptr`/raw `void*` initial-data signatures; `git diff --check` passed with only CRLF warnings.
- [x] T021 [P1] Upgrade `RHIDeviceCapabilities` from bool list to features, limits, and reasons. Evidence: `GraphicsBackendUtilsTests.DeviceCapabilitiesExposeFeatureReasonsAndStructuredLimits` first failed to compile because structured feature/reason/limit APIs were missing, then passed after adding `RHIDeviceFeature`, `RHIDeviceFeatureState`, `RHIDeviceLimits`, `RHIDeviceCapabilities::SetFeature()/GetFeature()`, and structured readiness diagnostics. DX12 capability construction now fills feature reasons for gated paths such as async compute/copy queue/transient allocation, runtime readiness helpers and threaded/DX12 queue readiness use feature queries, and legacy bool fields remain as compatibility mirrors. Validation: `cmake --build Build --target NullusUnitTests --config Debug`, `GraphicsBackendUtilsTests.*`, `ThreadedRenderingLifecycleTests.AsyncComputeDiagnosticsReportReadyButNotScheduledWhenQueueAndCapabilityExist:ThreadedRenderingLifecycleTests.AsyncComputeDiagnosticsReportDisabledWhenDedicatedQueueIsUnavailable`, runtime `rg` for direct `GetCapabilities().supports*`/`backendReady` reads, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T022 [P1] Expand `TextureFormat`, `TextureDimension`, and `SamplerDesc` expressiveness. Evidence: `RHITypesTests.*`, `DX12TextureUploadUtilsTests.Builds3DTexturePlanWithoutTreatingDepthAsArrayLayers`, `Builds2DArrayTexturePlanFromArrayLayers`, `DX12TextureViewUtilsTests.BuildsArraySrvAndRtvFor2DArrayColorTexture`, `Builds3DSrvForTexture3D`, and `DX12SamplerUtilsTests.*` first failed to compile because the new texture dimensions, formats, sampler fields, and DX12 sampler util did not exist; after adding expanded RHI enums/helpers, centralized DX12 format/sampler mapping, 2D array/3D view/upload support, and FrameGraph layer-count routing through `GetTextureLayerCount()`, `cmake --build Build --target NullusUnitTests --config Debug` and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RHITypesTests.*:DX12TextureUploadUtilsTests.*:DX12TextureViewUtilsTests.*:DX12SamplerUtilsTests.*:DX12ReadbackUtilsTests.*:FrameGraphSceneTargetsTests.FrameGraphCreatedResourcesRegisterAndRetireTransientLifetime` passed on 2026-05-09.
- [x] T023 [P1] Clarify or restrict RGB8-to-RGBA8 mapping semantics on DX12. Evidence: `DX12TextureUploadUtilsTests.DescribesPackedRgb8UploadAsOpaqueRgba8Expansion`, `BuildsRgb8PlanWithSeparateSourceAndNativeRowPitch`, `ExpandsPackedRgb8RowsToOpaqueRgba8Rows`, and `CopiesNativeMatchingUploadRowsDirectly` first failed to compile because the DX12 RGB8 upload semantics and row-copy contract were missing, then passed after documenting `RGB8` as packed RGB source data expanded to opaque native RGBA8 on DX12 and routing initial/update texture upload rows through `CopyDX12TextureUploadRow()`. Validation: `cmake --build Build --target NullusUnitTests --config Debug` and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DX12TextureUploadUtilsTests.*:DX12TextureViewUtilsTests.*:DX12ReadbackUtilsTests.*:RHITypesTests.*` passed on 2026-05-09.
- [x] T054 [P1] Change the base `RHIDevice::ReadPixelsChecked()` fallback so unimplemented backends return an explicit failure instead of fake success. Evidence: `RenderFrameworkContractTests.BaseRhiDeviceReadPixelsCheckedReportsUnsupportedInsteadOfFakeSuccess` passed on 2026-05-09 after the base fallback stopped calling the legacy void `ReadPixels()` and now returns `BackendFailure` with a diagnostic.
- [x] T055 [P1] Add status/error propagation for DX12 submit, queue wait/signal, and present failures instead of void-only logging. Evidence: `RenderFrameworkContractTests.QueueCheckedSubmissionAndPresentExposeStatus` passed on 2026-05-09 after adding `RHIQueueOperationResult`, `SubmitChecked()`, and `PresentChecked()`; DX12 queue wait/signal/present failures now return status and `RhiThreadCoordinator` uses the checked path for frame submit/present diagnostics.
- [x] T060 [P1] Make the base `RHIQueue::SubmitChecked()/PresentChecked()` fallback report unsupported instead of fake success. Evidence: `RenderFrameworkContractTests.BaseRhiQueueCheckedOperationsReportUnsupportedInsteadOfFakeSuccess` and `QueueCheckedSubmissionAndPresentExposeStatus` passed on 2026-05-09 after the base queue checked methods stopped calling the legacy `void` path and test queues that intentionally succeed override the checked API.
- [x] T061 [P1] Expose queue submit/present failures through driver/threaded telemetry instead of only logging them. Evidence: `RenderFrameworkContractTests.QueueOperationFailuresAreExposedThroughDriverTelemetry` passed on 2026-05-09 after `RhiThreadCoordinator` recorded failed checked queue operations into `DriverImpl`, `DriverRendererAccess::GetThreadedFrameTelemetry()`, and `RhiSubmissionFrame` telemetry fields.

## P1 / Pipeline And Materials

- [x] T024 [P1] Improve pipeline cache key with shader bytecode, layout, and toolchain fingerprint. Evidence: `PipelineCacheKeyTests.GraphicsKeyChangesWhenShaderBytecodeChanges`, `GraphicsKeyChangesWhenShaderToolchainFingerprintChanges`, `GraphicsKeyChangesWhenBindingLayoutChanges`, `GraphicsKeyChangesWhenRenderTargetLayoutChanges`, and `ShaderArtifactToolchainFingerprintIncludesProfileCacheKeyAndArtifactPath` first failed because shader toolchain fingerprint propagation and centralized graphics pipeline key builders were missing, then passed after moving graphics/compute key construction into `PipelineCache`, hashing shader bytecode/layout/render-target state, and filling `RHIShaderModuleDesc::shaderToolchainFingerprint` from compiled artifact platform/profile/entry/cache/artifact metadata. Validation: `cmake --build Build --target NullusUnitTests --config Debug`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=PipelineCacheKeyTests.*:ThreadedRenderingLifecycleTests.PipelineCacheUsesBackendAwareStableKeysAndTracksPrewarmStats`, and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DX12TextureUploadUtilsTests.*:DX12SamplerUtilsTests.*:RHITypesTests.*` passed on 2026-05-09.
- [x] T025 [P1] Use DX12 native cached PSO blobs to reduce cold-start PSO cost. Evidence: `DX12PipelineCacheBlobUtilsTests.BuildsStableSanitizedBlobPathsFromPipelineKey`, `WritesBlobThroughTemporaryFileAndReadsItBack`, `RejectsEmptyBlobWrites`, `BuildsD3D12CachedPipelineStateViewOnlyWhenBlobExists`, and `RetriesPipelineCreationOnlyWhenCachedBlobWasProvided` first failed because DX12 cached-blob helpers and retry policy were missing, then passed after adding `DX12PipelineCacheBlobUtils` and wiring DX12 graphics/compute PSO creation to read `CachedPSO`, retry without a stale blob on creation failure, and write `ID3D12PipelineState::GetCachedBlob()` back through temp-file atomic replace. Validation: `cmake -S . -B Build`, `cmake --build Build --target NullusUnitTests --config Debug`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DX12PipelineCacheBlobUtilsTests.*:PipelineCacheKeyTests.*:ThreadedRenderingLifecycleTests.PipelineCacheUsesBackendAwareStableKeysAndTracksPrewarmStats`, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T026 [P1] Extend pipeline state for independent blend, multiple render target color masks, MSAA, and more blend parameters. Evidence: `RHIPipelineStateUtilsTests.MapsLegacyPipelineStateToExpandedBlendAndMsaaState`, `DX12GraphicsPipelineUtilsTests.BuildsExpandedBlendStateForIndependentRenderTargets`, `BuildsRasterizerStateWithMsaaFlag`, `PipelineCacheKeyTests.GraphicsKeyChangesWhenExpandedBlendStateChanges`, and `GraphicsKeyChangesWhenRasterMsaaFlagChanges` first failed because RHI only exposed a single `blendState.enabled/colorWrite` pair and DX12 hardcoded RT0 blend/MSAA state, then passed after adding `RHIBlendFactor`, `RHIBlendOp`, `RHIColorWriteMask`, per-render-target blend descriptors, alpha-to-coverage, independent blend, raster multisample state, DX12 blend/raster builders, and pipeline-cache hashing for the expanded state. Validation: `cmake --build Build --target NullusUnitTests --config Debug`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RHIPipelineStateUtilsTests.*:DX12GraphicsPipelineUtilsTests.*:PipelineCacheKeyTests.*:DX12PipelineCacheBlobUtilsTests.*`, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T027 [P1] Establish systematic material/pass/pipeline variant keys. Evidence: `MaterialVariantKeyTests.BuildsStableMaterialIdentityFromAssetPath`, `BuildsRuntimeMaterialIdentityFromShaderAndRenderState`, and `BuildsPassAndPipelineVariantKeysFromMaterialPipelineAndOverrides` first failed because no shared material/pass/pipeline variant-key utility existed, then passed after adding `MaterialVariantKey` identity/pass key builders and routing deferred GBuffer material caching through the shared `DeferredGBuffer` pass variant key. Validation: `cmake -S . -B Build`, `cmake --build Build --target NullusUnitTests --config Debug`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=MaterialVariantKeyTests.*:DeferredSceneRendererMaterialCacheTests.*:PipelineCacheKeyTests.*`, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T028 [P1] Remove `Material::GetExplicitBindingSet()` dependency on global located driver. Evidence: `RendererFrameObjectBindingTests.MaterialExplicitBindingSetUsesProvidedDeviceInsteadOfLocatedDriver` first failed with a null binding set because `Material::GetExplicitBindingSet()` routed through the located driver instead of the provided `RHIDevice`, then passed after creating the explicit binding set directly on `device`; descriptor-lifetime guard tests `RendererFrameObjectBindingTests.ExplicitBindingSetCreationRequiresCentralDescriptorAllocator` and `ThreadedPreparedBindingSetCreationUsesCurrentFrameDescriptorAllocator` also passed in Debug targeted run on 2026-05-09.
- [x] T029 [P1] Add validation/diagnostic levels for missing bindings instead of silently writing null descriptors. Evidence: `RendererFrameObjectBindingTests.MaterialExplicitBindingSetReportsMissingRequiredBindings` and `MaterialExplicitBindingSetReportsNullSamplerDescriptor` first failed because `Material` had no explicit binding diagnostics and sampler creation failures could become null descriptor entries, then passed after adding `MaterialBindingDiagnosticSeverity`, `MaterialBindingDiagnostic`, `GetLastExplicitBindingDiagnostics()`, and `HasExplicitBindingErrors()`; missing buffers, missing texture parameters, null texture/view descriptors, and null sampler descriptors are now recorded as errors and skipped. Validation: Debug build plus targeted `RendererFrameObjectBindingTests` material/descriptor tests passed on 2026-05-09.
- [x] T030 [P1] Add stricter shader-reflection validation for binding layouts and sets. Evidence: `ShaderBindingLayoutUtilsTests.ValidatesConflictingReflectionBindingsBeforeLayoutCreation` first failed because no shader-binding reflection validation API existed, then passed after adding `ValidateShaderBindingReflection()` with error diagnostics for duplicate same-register-class descriptor slots, invalid non-positive array sizes, unnamed bindings, and zero-sized constant buffers; `RendererFrameObjectBindingTests.MaterialExplicitBindingSetReportsShaderReflectionValidationErrors` passed after `Material` propagated validation errors into explicit binding diagnostics and stopped layout/set creation for invalid reflection. Compatibility tests confirmed texture `t#` and sampler `s#` sharing the same numeric register remain valid. Validation: Debug build plus `ShaderBindingLayoutUtilsTests.*` and targeted material binding diagnostics tests passed on 2026-05-09.

## P1 / FrameGraph

- [x] T031 [P1] Split `SceneRenderGraphBuilder.cpp` responsibilities across forward, deferred, light-grid, and threaded concerns. Evidence: `SceneRenderGraphBuilderStructureTests.BuilderFacadeIncludesSeparatedForwardDeferredAndLightGridModules` first failed because `SceneRenderGraphBuilder` was still a single implementation surface, then passed after splitting the facade into `SceneRenderGraphBuilderForward.{h,cpp}`, `SceneRenderGraphBuilderDeferred.{h,cpp}`, `SceneRenderGraphBuilderLightGrid.{h,cpp}`, and shared `SceneRenderGraphBuilderInternal.h`; the original `SceneRenderGraphBuilder.h/.cpp` now acts as compatibility facade. Validation: `cmake -S . -B Build`, Debug `NullusUnitTests` build, `SceneRenderGraphBuilderStructureTests.*`, `FrameGraphSceneTargetsTests.SceneRenderGraphBuilderExposesExpectedForwardAndDeferredOrdering`, `PrepareForwardSceneGraphImportsExternalTargetsAndPreservesForwardPassOrder`, forward/deferred threaded offscreen package tests, and deferred/compute visibility barrier tests passed on 2026-05-09.
- [x] T032 [P1] Add FrameGraph pass/resource compile-time validation. Evidence: `FrameGraphSceneTargetsTests.ValidateThreadedExecutionInputsReportsPassAndResourceContractErrors`, `ValidateThreadedExecutionInputsAcceptsCompletePassAndResourceContracts`, and `BuildThreadedExecutionPlanRejectsInvalidResourceContractsBeforePlanning` first failed to compile because no FrameGraph compile-validation API existed, then passed after adding `FrameGraphCompileDiagnostic`/`FrameGraphCompileValidationResult`, validation for empty graph pass names, missing metadata, and null buffer/texture resource or visibility contracts, and a pre-planning validation gate in `BuildThreadedRenderSceneExecutionPlan()`. Validation: Debug `NullusUnitTests` build plus targeted FrameGraph threaded-plan, compute compile, builder ordering, and forward prepare tests passed on 2026-05-09.
- [x] T033 [P1] Pass stable debug names for FrameGraph transient resources. Evidence: `FrameGraphSceneTargetsTests.SceneColorTargetSupportsSamplingForEditorViews` and `FrameGraphCreatedResourcesRegisterAndRetireTransientLifetime` first failed because scene target descriptors had no semantic `debugName` and `FrameGraphTexture`/`FrameGraphBuffer` created generic `"FrameGraphTexture"`/`"FrameGraphBuffer"` RHI resources; after adding `FrameGraphBuffer::Desc::debugName`, preserving `FrameGraphTexture::Desc::debugName` for transient textures and views, and naming scene/GBuffer descriptors, `DeferredSceneGraphImportsGBufferResourcesWithStableDebugNames`, transient lifetime, barrier tracking, and forward prepare tests passed in Debug targeted run on 2026-05-09.
- [x] T034 [P1] Validate illegal threaded pass metadata combinations, missing resources, and bad queue dependencies. Evidence: `FrameGraphSceneTargetsTests.ValidateThreadedExecutionInputsReportsIllegalMetadataAndQueueContracts` and `BuildThreadedExecutionPlanRejectsIllegalMetadataContractsBeforePlanning` first failed because the validation layer did not know about illegal recorded-output propagation, compute pass queue/output constraints, or missing `LastCompute`/`LastGraphics` sources; after extending `FrameGraphCompileDiagnosticCode` and `ValidateThreadedRenderSceneExecutionInputs()`, these diagnostics are reported before planning. Regression validation: `ValidateThreadedExecutionInputsAcceptsCompletePassAndResourceContracts`, prepared-compute metadata/compile tests, deferred resource dependency tests, forward prepare, and threaded deferred/compute visibility barrier tests passed in Debug targeted runs on 2026-05-09.
- [x] T035 [P1] Integrate UI composition into the formal FrameGraph or define a clear synchronization boundary. Evidence: `ThreadedRenderingLifecycleTests.UiCompositionSyncBoundaryDescribesSceneUiAndPresentOrdering` first failed to compile because `DriverUIAccess::BuildUICompositionSyncBoundary()` and `SetUICompositionSignal()` did not exist, then passed after adding an explicit scene-to-UI and UI-to-present boundary contract; `Editor::RenderEditorUI()` now consumes that boundary and standalone UI present clears the UI signal after finalization so stale UI fences cannot leak to the next frame. Validation: Debug `NullusUnitTests` build, the new UI composition boundary test, existing threaded UI lifecycle present/prepare tests, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T036 [P1] Reduce duplicate submit/present logic between standalone UI frames and ordinary explicit frames. Evidence: `ThreadedRenderingLifecycleTests.StandaloneExplicitFramePresentUsesUiCompositionBoundaryAndClearsSignal` first failed because ordinary standalone explicit present dropped the UI composition signal and retained stale UI sync state; after extracting shared standalone submit, present-desc, present wrapper, and UI-signal clear helpers in `RhiThreadCoordinator`, both standalone UI and ordinary explicit frames use the same present contract. Validation: Debug `NullusUnitTests` build, the new test, T035 UI boundary test, existing standalone/threaded UI present tests, selected threaded submission/readback regressions, and `git diff --check` passed with only CRLF warnings on 2026-05-09.

## P2 / Performance

- [x] T037 [P2] Optimize mesh build path to avoid `Geometry::Vertex` to `vector<float>` second copy. Evidence: `MeshBoundingSphereTests.BuildsVertexUploadViewDirectlyFromStructuredVertices` first failed to compile because no direct structured vertex upload view existed, then passed after adding `BuildMeshVertexUploadView()` and routing `Mesh::CreateBuffers()` through `VertexBuffer<Geometry::Vertex>` instead of flattening each `Geometry::Vertex` into a temporary `std::vector<float>`; `VertexBuffer` now accepts const source data. Validation: Debug `NullusUnitTests` build, `MeshBoundingSphereTests.*`, `ModelBoundingSphereTests.*`, mesh-using renderer smoke tests (`RendererStatsTests.*`, `PanelWindowHookTests.*`, `DeferredSceneRendererMaterialCacheTests.*`), `rg` confirmed no old flattened mesh vertex upload path remains, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T038 [P2] Batch short-lived GPU operations for descriptors, uploads, and readbacks. Evidence: `UploadContextTests.SubmitUploadBatchReturnsSingleCompletionForAcceptedShortLivedRequests` and `SubmitUploadBatchRejectsInvalidRequestWithIndexedDiagnostic` first failed because upload batch request/submission APIs were missing, then passed after adding `UploadBatchRequest`, `UploadBatchSubmission`, and a shared batch validation/completion path; `DescriptorAllocatorTests.AllocateBatchReservesTransientDescriptorsAsOneShortLivedGroup` first failed because descriptor batch allocation was missing, then passed after adding `DescriptorAllocationBatch` and a one-lock `AllocateBatch()` path. Readback short-lived object churn remains covered by the existing reusable DX12 readback context and `DX12ReadbackUtilsTests.PlansScratchReadbackResourceReuseUntilCapacityIsExceeded`. Validation: Debug `NullusUnitTests` build, `UploadContextTests.*`, `DescriptorAllocatorTests.*`, `DX12ReadbackUtilsTests.*`, selected threaded upload/readback lifecycle tests, and `git diff --check` passed with only CRLF warnings on 2026-05-09.
- [x] T039 [P2] Move hot-path logging behind diagnostics/trace switches. Evidence: `LightingDataProviderTests.LightGridHotPathFailureDiagnosticsAreControlledByRenderDrawTraceSwitch` first failed because `LightGridPrepass` had no hot-path diagnostics policy API, then passed after routing per-frame LightGrid prepare/prepared-compute failure logs through `logRenderDrawPath`; `LightingDataProviderTests.*` and `RendererStatsTests.*:PanelWindowHookTests.*:DeferredSceneRendererMaterialCacheTests.*` passed in Debug targeted runs on 2026-05-09, with the prior LightGrid error spam absent from default test output.
- [x] T040 [P2] Unify descriptor allocator responsibilities with DX12 shader-visible heap allocator. Evidence: `DescriptorAllocatorTests.DescriptorRangeAllocatorProvidesBoundedPolicyForShaderVisibleHeaps` first failed because the shared bounded descriptor range allocator/persistent-capacity stats did not exist, then passed after extracting `DescriptorRangeAllocator` from the default allocator and routing `DX12ShaderVisibleDescriptorHeapAllocator` offset allocation/free through the same policy while leaving native heap/handle ownership in DX12. Validation: Debug `NullusUnitTests` build, `DescriptorAllocatorTests.*`, descriptor lifetime/binding tests (`RendererFrameObjectBindingTests.*Descriptor*`, `ThreadedRenderingLifecycleTests.*Descriptor*`) passed on 2026-05-09.
- [x] T041 [P2] Audit `UploadContext` ring allocator head/tail overlap and capacity policy. Evidence: `UploadContextTests.RingAllocatorRejectsWrapThatWouldOverlapUnretiredAllocations` first failed because `DefaultUploadContext::Allocate()` wrapped to offset 0 and overlapped an unretired allocation; after adding explicit retired-range overlap checks and non-recursive wrap/capacity handling, `UploadContextTests.RingAllocatorRejectsWrapThatWouldOverlapUnretiredAllocations` and `RingAllocatorRejectsRequestsLargerThanCapacity` passed. Validation: Debug `NullusUnitTests` build, `UploadContextTests.*`, selected threaded upload/readback lifecycle tests, and DX12 texture upload/update utility tests passed on 2026-05-09.
- [x] T042 [P2] Reduce main-thread stalls from synchronous external shader compilation/reflection processes. Evidence: `ShaderCompilerTests.CompileBatchRunsIndependentStagesConcurrentlyAndPreservesOrder` first failed because `ShaderCompiler::CompileBatch()` did not exist, then passed after adding batch compile/reflect helpers that dispatch independent stage work asynchronously while preserving result order; `ShaderLoader::CreateHLSLShaderAsset()` now queues VS/PS/CS DXIL/SPIR-V compile work and DXIL/SPIR-V reflection work through those helpers instead of serial compile/reflect calls. Validation: Debug `NullusUnitTests` build, `ShaderCompilerTests.*`, shader fingerprint/cache-key tests, ShaderLoader-dependent material/binding/deferred cache tests, DX12 pipeline utility/cache-blob tests, and focused LightGrid renderer package tests passed on 2026-05-09.
- [x] T056 [P2] Route LightGrid compute pipeline cache keys through the same bytecode/toolchain/layout fingerprint contract as the main `PipelineCache`. Evidence: `PipelineCacheKeyTests.ComputeKeyChangesWhenShaderBytecodeAndToolchainChange` and `PipelineCacheKeyTests.*` passed on 2026-05-09 after removing the LightGrid-local hash and building the cache key from `RHIComputePipelineDesc` via the shared `BuildComputePipelineCacheKey()`.
- [x] T057 [P2] Define or implement real backend execution semantics for `UploadContext::SubmitUploadBatch()` instead of an immediate validation-only token. Evidence: `UploadContextTests.SubmitUploadBatchReturnsSingleCompletionForAcceptedShortLivedRequests` and `UploadContextTests.*` passed on 2026-05-09 after `UploadBatchSubmission` gained `recordedBackendWork`; the default upload context now explicitly reports validation-only acceptance with `recordedBackendWork=false` instead of implying backend copy/barrier work was recorded.
- [x] T058 [P2] Add bounded watchdogs/diagnostics around swapchain resize drain and synchronous threaded lifecycle drain loops. Evidence: `RenderFrameworkContractTests.SwapchainResizeKeepsPendingStateWhenFrameFenceDrainFails`, `RenderFrameworkContractTests.*`, and `DriverSwapchainResizeTests.*` passed on 2026-05-09 after swapchain resize frame-fence drains became bounded and keep pending/resource state on timeout; synchronous threaded lifecycle drain now has a 5s watchdog and logs timeout diagnostics instead of sleeping forever.
- [x] T062 [P2] Make synchronous threaded drain failure actionable to callers while preserving the legacy void wrapper. Evidence: `RenderFrameworkContractTests.ThreadedDrainResultIsActionableForCallers` and selected threaded lifecycle tests passed on 2026-05-09 after adding `TryDrainThreadedRendering()` to renderer/test access and gating resize follow-up on a successful drain.
- [x] T063 [P2] Let upload callers require real backend-recorded work so the validation-only default context cannot be mistaken for an execution layer. Evidence: `UploadContextTests.SubmitUploadBatchCanRequireRealBackendRecording` and `UploadContextTests.*` passed on 2026-05-09 after adding `UploadBatchRequest::requireRecordedBackendWork`; `DefaultUploadContext` now returns a failed token and diagnostic when real backend recording is required.
- [x] T064 [P2] Invalidate LightGrid member pipeline caches when the shared compute pipeline cache key changes. Evidence: `RenderFrameworkContractTests.LightGridPipelineMemberCacheTracksPipelineCacheKey`, `PipelineCacheKeyTests.*`, and `LightingDataProviderTests.*` passed on 2026-05-09 after `LightGridPrepass` started storing injection/compact pipeline keys and no longer short-circuits solely on non-null pipeline pointers.
- [x] T065 [P1] Make threaded/standalone frame-fence waits use a bounded driver policy with diagnostics instead of default infinite waits. Evidence: `RenderFrameworkContractTests.StandaloneFrameFenceWaitUsesBoundedDriverPolicy` passed on 2026-05-09 after `RhiThreadCoordinator` routed standalone UI, standalone explicit, threaded begin, and threaded retirement frame-fence waits through a shared 5s driver wait policy with diagnostics.
- [x] T066 [P1] Add frame-scoped queue-operation failure telemetry so callers can distinguish current-frame failures from historical cumulative failures. Evidence: `RenderFrameworkContractTests.QueueOperationTelemetrySeparatesCurrentFrameFromCumulativeHistory` passed on 2026-05-09 after adding `currentFrameQueueOperationFailureCount` and `currentFrameLastQueueOperationFailure` to driver/threaded telemetry and resetting them at frame begin.
- [x] T067 [P1] Promote queue submit/present failure telemetry toward an explicit device-lost/recovery state contract. Evidence: `RenderFrameworkContractTests.DeviceLostQueueOperationIsExposedThroughTelemetry` passed on 2026-05-09 after adding `RHIQueueOperationStatusCode::DeviceLost`, exposing `deviceLostDetected`/`deviceLostReason` in telemetry, and mapping DX12 device-removed submit/present paths to the new status.
- [x] T068 [P1] Clarify `RHICompletionToken` polling/finalization semantics so side-effecting async readback completion is explicit rather than hidden behind const status queries. Evidence: `RenderFrameworkContractTests.CompletionTokenExposesExplicitPollContract`, `DX12ReadbackUtilsTests.*`, and `UploadContextTests.*` passed on 2026-05-09 after adding explicit `RHICompletionToken::Poll()` and moving DX12 readback polling/finalization behind that contract while keeping compatibility `GetStatus()`/`IsComplete()` wrappers.
- [x] T069 [P1] Check and diagnose TimelineProfiler GPU query fence `SetEventOnCompletion()` failures instead of ignoring HRESULT. Evidence: `RenderFrameworkContractTests.TimelineProfilerFenceCompletionFailureIsChecked` passed on 2026-05-09 after `GPUProfiler::QueryHeap::Reset()` captured the HRESULT and skipped allocator reset on failure.
- [x] T070 [P2] Remove or backend-scope the remaining raw `NativeDX12Texture::GetNativeTextureHandle()` escape hatch. Evidence: `RenderFrameworkContractTests.DX12TextureDoesNotExposeRawVoidNativeTextureHandle` passed on 2026-05-09 after removing the public raw `void*` texture accessor and leaving typed RHI native handles plus backend-private `GetResource()`.
- [x] T071 [P2] Add shader generation/version contract for hot reload so cached shader modules and LightGrid member pipelines invalidate even when asset reload paths bypass current setters. Evidence: `RenderFrameworkContractTests.ShaderGenerationParticipatesInExplicitModuleCacheKey`, `PipelineCacheKeyTests.*`, and `LightingDataProviderTests.*` passed on 2026-05-09 after `Shader` gained `GetGeneration()`, increments generation on reflection/artifact changes, and includes generation in the explicit shader-module cache key.
- [x] T072 [P2] Reuse LightGrid per-frame scratch buffers to reduce vector allocation churn for packed lights and cluster data. Evidence: `RenderFrameworkContractTests.LightGridPrepassReusesFrameScratchStorage`, `PipelineCacheKeyTests.*`, and `LightingDataProviderTests.*` passed on 2026-05-09 after `LightGridPrepass` moved packed frame data into reusable member scratch storage and clears/reserves per-frame vectors instead of rebuilding all storage from a local scratch object.
- [x] T073 [P2] Replace remaining source-grep render contract tests with behavioral tests where practical. Evidence: `RenderFrameworkContractTests.CompletionTokenExposesExplicitPollContract` now exercises the explicit `Poll()` compatibility contract directly, `DX12TextureDoesNotExposeRawVoidNativeTextureHandle` is a compile-time API contract instead of source scanning, and `ThreadedDrainResultIsActionableForCallers` now calls `DriverTestAccess::TryDrainThreadedRendering()` and `DriverRendererAccess::TryDrainThreadedRendering()` on a threaded driver. Remaining DX12 fence helper, TimelineProfiler fence-failure, Shader private generation, and LightGrid private cache/scratch guards intentionally stay as static regression checks because replacing them would require backend failure injection or opening private production state solely for tests. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*` passed on 2026-05-09.
- [x] T074 [P2] Implement a real DX12 backend upload executor behind `UploadContext::SubmitUploadBatch()` so callers requiring recorded backend work can use the unified upload path. Evidence: added backend `UploadBackend`/`CreateBackendUploadContext()`, DX12 staging-buffer backend factory, command-buffer-recorded buffer/texture barriers and copies, explicit texture upload pitch propagation, and per-frame staging-buffer keepalive/retirement. `Driver` now creates the DX12 backend upload context for explicit DX12 devices while non-DX12 paths keep validation-only semantics. Validation: Debug `NullusUnitTests` build, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=UploadContextTests.*`, and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*` passed on 2026-05-09.
- [x] T075 [P2] Make backend-recorded upload batches all-or-nothing at command-buffer recording time. Evidence: `UploadContextTests.BackendUploadContextDoesNotRecordPartialBatchWhenStagingCreationFails` first failed because the first upload in a batch had already recorded two barriers and one copy before the second staging buffer creation failed; after splitting `BackendUploadContext::SubmitUploadBatch()` into staging preparation and command recording phases, any staging creation failure returns a failed completion token before recording barriers/copies. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=UploadContextTests.*` passed on 2026-05-09.
- [x] T076 [P1] Let backend upload completion tokens optionally track a submitted GPU fence instead of only reporting command-buffer recording completion. Evidence: `UploadContextTests.BackendUploadContextCompletionCanTrackSubmittedGpuFence` first failed to compile because `UploadBatchRequest` had no completion fence contract, then passed after adding optional `completionFence` and a fence-backed upload completion token whose `Poll()` stays pending until the fence is signaled and whose `Wait()` delegates timeout to the fence. Existing recorded-only callers keep the immediate recorded-work token. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=UploadContextTests.*` passed on 2026-05-09.
- [x] T077 [P1] Replace threaded render/RHI worker idle `sleep_for(1ms)` polling with event-driven wakeups. Evidence: `RenderFrameworkContractTests.ThreadedWorkersUseWakeConditionInsteadOfIdleSleepPolling` first failed because `Driver` had no worker wake condition and both render/RHI workers still idled with `sleep_for(1ms)`, then passed after adding a shared threaded worker wake condition/generation counter and notifying workers on frame publish, render-scene completion, RHI retirement, pause, and shutdown. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*:ThreadedRenderingLifecycleTests.*` passed on 2026-05-09.
- [x] T078 [P1] Move DX12 upload backend creation out of `Driver.cpp` so Context no longer depends on DX12 native device casts. Evidence: `RenderFrameworkContractTests.DriverCreatesUploadContextThroughBackendFactoryWithoutDX12Casts` first failed because `Driver.cpp` included `DX12Resource.h`, cast `ID3D12Device*`, and called `CreateDX12UploadBackend()` directly; after adding `Backend::CreateUploadContextForRhiDevice()` in the RHI backend factory, `Driver` creates frame upload contexts through the factory and no longer has DX12 upload-backend type knowledge. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=UploadContextTests.*:RenderFrameworkContractTests.*` passed on 2026-05-09.
- [x] T079 [P2] Add owner metadata, timeout diagnostics, and stale lock recovery to shader artifact locking. Evidence: `ShaderCompilerTests.ShaderArtifactLockReportsOwnerAndRecoversStaleLocks` first failed to link because the lock-options API did not exist, then exposed that owner metadata made lock cleanup require recursive removal, and finally passed after shader artifact locks wrote `owner.txt`, reported active owner/waiter on timeout, and removed stale lock directories before retrying. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ShaderCompilerTests.*` passed on 2026-05-09.
- [x] T080 [P2] Replace shader process output polling with overlapped/job-object process management that can terminate child process trees. Evidence: `ShaderCompilerTests.ShaderCompilerProcessUsesJobObjectInsteadOfPipePolling` first failed because `ExecuteShaderCompilerProcess()` still used `PeekNamedPipe` and `WaitForSingleObject(..., 5)` with no job object; after switching Windows shader process execution to a cleanup Job Object plus blocking reader thread, the static contract passed. `ShaderCompilerTests.ShaderCompilerProcessTimeoutTerminatesChildProcessTree` verifies timeout does not leave a child process alive to write a marker file. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ShaderCompilerTests.*` passed on 2026-05-09.
- [x] T081 [P2] Refactor TimelineProfiler GPU resource ownership toward RAII/ComPtr and explicit readback unmap. Evidence: `RenderFrameworkContractTests.TimelineProfilerGpuResourcesUseRaiiAndUnmapReadback` first failed because TimelineProfiler used raw DX12 query/command/readback/fence pointers, manually deleted command-list states, released readback without unmapping, and released allocators by hand; after moving GPU query resources to `Microsoft::WRL::ComPtr`, changing command-list state storage to `std::unique_ptr`, and explicitly unmapping readback on shutdown, the contract passed. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*` passed on 2026-05-09.
- [x] T082 [P2] Expand FrameGraph compile-time validation for undeclared resource usage, read/write conflicts, and invalid queue dependencies. Evidence: `FrameGraphSceneTargetsTests.ValidateThreadedExecutionInputsReportsResourceConflictsAndInvalidDependencies` first failed because the validator had no conflict/invalid-dependency diagnostic codes or plan-level dependency validator; after adding `ConflictingBufferResourceAccess`, `ConflictingTextureResourceAccess`, `InvalidQueueDependency`, same-pass read/write conflict checks, and `ValidateThreadedRenderSceneExecutionPlan()`, the test passed. `BuildThreadedRenderSceneExecutionPlan()` now validates generated dependency edges before returning. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=FrameGraphSceneTargetsTests.*` passed on 2026-05-09.
- [x] T083 [P3] Continue reducing raw `void*` in `NativeRenderDeviceInfo`, platform window plumbing, and RenderDoc bridge boundaries. Evidence: `UIAndToolingBackendAwarenessTests.NativeRenderDeviceInfoExposesTaggedHandlesForToolingBoundaries` passed on 2026-05-09 after adding lightweight tagged `NativeRenderDeviceHandle` accessors to `NativeRenderDeviceInfo` and a typed `ResolveRenderDocCaptureDeviceHandle()` tooling resolver while keeping the legacy raw pointer resolver as a compatibility wrapper. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=UIAndToolingBackendAwarenessTests.*:RenderFrameworkContractTests.*` passed on 2026-05-09.
- [x] T084 [P2] Split FrameGraph pass resource access semantics so prepared compute lifecycle barriers are not mixed with cross-pass dependency declarations. Evidence: `FrameGraphSceneTargetsTests.PreparedComputeThreadedPassInputCarriesShaderReadAccessExports` and `PreparedComputeLifecycleBufferAccessesDoNotTripConflictValidation` first failed because prepared compute lifecycle buffers were still emitted into `bufferResourceAccesses`; after routing before/UAV-after barriers to `bufferVisibilityTransitions` and shader-read-after exports to `exportedBufferVisibilityTransitions`, both passed while `ThreadedResourceDependencyUsesLastWriteAccessState` preserved explicit cross-pass write dependency behavior. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=FrameGraphSceneTargetsTests.*` passed on 2026-05-09.
- [x] T085 [P1] Add non-throwing FrameGraph threaded compile result paths so render-thread compile failures surface as diagnostics/fallback instead of uncaught `std::invalid_argument`. Evidence: `FrameGraphSceneTargetsTests.TryBuildThreadedExecutionPlanReturnsDiagnosticsInsteadOfThrowing` first failed to compile because no result-returning plan builder existed; `TryCompileAndApplyThreadedExecutionReportsDiagnosticsWithoutMutatingPackage` first failed to compile because no non-throwing compile/apply entry point existed. Both passed after adding `ThreadedRenderSceneExecutionPlanBuildResult`, `ThreadedRenderSceneExecutionCompileResult`, `TryBuildThreadedRenderSceneExecutionPlan()`, `TryCompileThreadedRenderSceneExecution()`, and `TryCompileAndApplyThreadedRenderSceneExecution()` while keeping legacy throwing wrappers. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=FrameGraphSceneTargetsTests.*` passed on 2026-05-09.
- [x] T086 [P2] Replace synchronous threaded lifecycle drain `sleep_for(1ms)` polling with the existing worker wake condition. Evidence: `RenderFrameworkContractTests.ThreadedWorkersUseWakeConditionInsteadOfIdleSleepPolling` first failed because `DrainThreadedLifecycleSynchronously()` still contained `std::this_thread::sleep_for(std::chrono::milliseconds(1))`; after routing the no-progress wait through `WaitForThreadedWorkerWake()` with the observed generation, the contract passed. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*:ThreadedRenderingLifecycleTests.*:LightingDataProviderTests.*` passed on 2026-05-09.
- [x] T087 [P1] Split synchronous threaded lifecycle drain wake timeout from the background worker idle wake timeout to avoid camera/navigation hitches when a frame-path drain races a worker-owned slot. Evidence: `RenderFrameworkContractTests.ThreadedWorkersUseWakeConditionInsteadOfIdleSleepPolling` first failed because `DrainThreadedLifecycleSynchronously()` used the same `kThreadedWorkerWakeTimeout` path as idle render/RHI workers; after adding `kThreadedDrainWakeTimeout` and passing it only from the drain path, worker idling keeps the 250ms condition wait while synchronous drain remains bounded to the old short-latency cadence. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.ThreadedWorkersUseWakeConditionInsteadOfIdleSleepPolling` passed on 2026-05-09.
- [x] T088 [P1] Avoid forcing ordinary offscreen external-output view frames to wait for GPU completion immediately after submit; keep the pre-reuse frame-context wait for resource lifetime, but let Scene/Game View texture presentation consume the latest retired output asynchronously when no readback/resize/synchronized-presentation drain is requested. Evidence: `EditorRenderPathContractTests.SceneViewCameraControlDoesNotForceRetirementDrain` passed on 2026-05-09 after Scene View camera control stopped requesting synchronized retired-frame presentation; `RhiThreadCoordinator` now skips immediate threaded submit fence waits for ordinary external-output frames without readback. Runtime validation with `App\Win64_Debug_Runtime_Shared\Editor.exe --editor-performance-mode --log-editor-fps --editor-log-view-timings TestProject\TestProject.nullus` showed Scene View frames around 70-80 FPS with `end` around 6-7ms instead of the earlier 40 FPS helper-only path.
- [x] T089 [P1] Skip clustered LightGrid preparation for editor helper-only Scene View frames. Evidence: `EditorRenderPathContractTests.EditorHelperOnlySnapshotSkipsLightGridPreparation` passed on 2026-05-09 after editor helper-only snapshots stopped building `BuildLightGridCompileContext()`; runtime renderer timing showed `lightGrid=0.000ms` and empty Scene View improved from roughly 40-50 FPS to roughly 70-80 FPS.
- [x] T090 [P2] Throttle inactive Game View edit-mode preview rendering so visible but non-interactive Game View does not duplicate the full Scene View render cost every frame. Evidence: `EditorRenderPathContractTests.GameViewEditPreviewThrottlesWhenInactive` and `GameViewRendersImmediatelyWhenInteractivePlayingOrResized` passed on 2026-05-09 after adding `GameViewRenderPolicy`; focused/hovered, playing, resized, first-frame, and scene-change cases still render immediately while inactive edit preview falls back to a bounded idle refresh interval.

## P2 / Tooling And Ease Of Use

- [x] T043 [P2] Add DXC version, paths, and compiler argument version to shader cache keys. Evidence: `ShaderCompilerTests.ShaderCompilationCacheKeyIncludesDxcIdentityAndArgumentSchema` first failed before a cache-key helper exposed toolchain identity fields, then passed after adding `ShaderCompilerToolchainIdentity`/`BuildShaderCompilationCacheKey()` and routing DXC compilation cache keys through source dependency fingerprints plus canonical compiler path, compiler file fingerprint, include paths, macros, and `dxc-args-v1`. Validation: Debug `NullusUnitTests` build plus `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ShaderCompilerTests.*:PipelineCacheKeyTests.*:DX12PipelineCacheBlobUtilsTests.*` and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RendererFrameObjectBindingTests.*:DeferredSceneRendererMaterialCacheTests.*:MaterialVariantKeyTests.*` passed on 2026-05-09.
- [x] T044 [P2] Write shader artifacts through temp file plus atomic rename/lock to prevent concurrent corruption. Evidence: `ShaderCompilerTests.ShaderArtifactStagingPlanUsesSameDirectoryTempFileAndLock` first failed because no staging-plan API existed, and `ShaderArtifactTextWritesCommitThroughTemporaryFile` first failed because shader artifact cache writes had no atomic helper; after adding `ShaderArtifactStagingPlan`, lock-directory guarded commit, unique same-directory temp artifacts, and `WriteShaderArtifactTextAtomic()`, DXC `-Fo` now targets a staged temp artifact and commits to the final cache path only after successful compilation, while reflection and GLSL cache writes use the same atomic text helper. Validation: Debug `NullusUnitTests` build, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ShaderCompilerTests.*:PipelineCacheKeyTests.*:DX12PipelineCacheBlobUtilsTests.*`, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RendererFrameObjectBindingTests.*:DeferredSceneRendererMaterialCacheTests.*:MaterialVariantKeyTests.*`, and `rg` for shader artifact write sites passed on 2026-05-09.
- [x] T045 [P2] Add timeout, cancellation, and clearer diagnostics to shader compile/reflection processes. Evidence: `ShaderCompilerTests.ShaderCompilerProcessReportsCommandLineAndOutputForSucceededProcess`, `ShaderCompilerProcessTimeoutReturnsDiagnostics`, and `ShaderCompilerProcessCancellationReturnsDiagnostics` first failed because shader compiler process execution was an internal infinite-wait helper with no timeout/cancel/result status API; after adding `ShaderProcessOptions`, `ShaderProcessResult`, `ShaderProcessStatus`, and `ExecuteShaderCompilerProcess()`, DXC compile and reflection dumpbin calls use the shared executor with command-line diagnostics, process status, timeout handling, and cancellation support. Validation: Debug `NullusUnitTests` build, `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=ShaderCompilerTests.*:PipelineCacheKeyTests.*:DX12PipelineCacheBlobUtilsTests.*:RendererFrameObjectBindingTests.*:DeferredSceneRendererMaterialCacheTests.*:MaterialVariantKeyTests.*`, and `rg` confirmed no old `ExecuteProcess`/`ExecuteCommand`/shader-compiler infinite wait helper remains on 2026-05-09.
- [x] T046 [P2] Improve RenderDoc/PIX resource, pass, and pipeline names. Evidence: `DX12DebugNameUtilsTests.BuildsSpecificTextureAndBufferLabelsForCaptureTools` and `BuildsPipelineLabelsWithStableKeyAndShaderNames` first failed before centralized DX12 capture-label builders existed, then passed after adding `DX12DebugNameUtils`; DX12 buffers/textures now set object names with backend, resource kind, semantic name, dimensions/format/usage, texture view descriptor heaps carry source texture/view labels, and root signatures plus graphics/compute PSOs include semantic pipeline names, shader names, and pipeline cache key labels for RenderDoc/PIX. Validation: `cmake -S . -B Build`, Debug `NullusUnitTests` build, and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=DX12DebugNameUtilsTests.*:DX12GraphicsPipelineUtilsTests.*:DX12PipelineCacheBlobUtilsTests.*:PipelineCacheKeyTests.*:DX12TextureUploadUtilsTests.*:DX12TextureViewUtilsTests.*:FrameGraphSceneTargetsTests.DeferredSceneGraphImportsGBufferResourcesWithStableDebugNames:FrameGraphSceneTargetsTests.FrameGraphCreatedResourcesRegisterAndRetireTransientLifetime` passed on 2026-05-09.
- [x] T047 [P2] Establish clearer phase gates and fallback diagnostics for multi-backend support. Evidence: `GraphicsBackendUtilsTests.BackendPhaseGateReportExplainsUnsupportedBackendFallback` and `BackendPhaseGateReportIncludesMissingCapabilityReason` first failed because there was no structured phase-gate report API; after adding `RuntimeConsumer`, `BackendPhaseGate`, `BackendPhaseGateSeverity`, `BackendPhaseGateReport`, and `EvaluateBackendPhaseGate()`, unsupported backend selection, device-creation readiness, and capability-validation failures report requested backend, consumer, fallback backend, gate phase, severity, and reason. `Driver` fallback logs now include the same phase-gate summary when rejecting non-DX12 phase-1 backends or continuing without an explicit device. Validation: Debug `NullusUnitTests` build and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=GraphicsBackendUtilsTests.*:DriverNullDeviceReadinessTests.*:UIAndToolingBackendAwarenessTests.*` passed on 2026-05-09.
- [x] T048 [P2] Add contract tests for resize, readback, UI composition, and resource lifetime. Evidence: added `RenderFrameworkContractTests` covering swapchain resize drain/backbuffer release, async readback begin token vs checked wait diagnostics, UI composition signal propagation/clear, and FrameGraph transient resource retirement; `cmake -S . -B Build`, Debug `NullusUnitTests` build, and `Build\bin\Debug\NullusUnitTests.exe --gtest_filter=RenderFrameworkContractTests.*` passed on 2026-05-09.

## Current Session Validation Notes

- Debug build command timed out once after relinking, but `Build/bin/Debug/NullusUnitTests.exe` timestamp updated and targeted `TextureResourceLifecycleTests.*` passed at 2026-05-08 19:58.
- Before broad completion, rerun:

```powershell
cmake --build Build --target NullusUnitTests --config Debug
Build\bin\Debug\NullusUnitTests.exe --gtest_filter=TextureResourceLifecycleTests.*
Build\bin\Debug\NullusUnitTests.exe
git diff --check
```

## Execution Order

1. Finish any `[~]` or `[!]` items in P0/P1 resource correctness that already have tests in progress.
2. Complete remaining P0 tasks in listed order unless a dependency requires reordering.
3. Move through P1 by resource/synchronization, RHI architecture, pipeline/material, then FrameGraph.
4. Complete P2 performance/tooling after correctness and contracts are stable.
